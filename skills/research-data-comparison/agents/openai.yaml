interface:
  display_name: "Research Data Comparison"
  short_description: "Compare data with units, pairing, baselines, and uncertainty"
  default_prompt: "Use $research-data-comparison to audit whether these results are comparable and what claims the differences support."
policy:
  allow_implicit_invocation: true
