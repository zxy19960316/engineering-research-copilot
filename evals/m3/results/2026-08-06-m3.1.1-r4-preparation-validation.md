# M3.1.1 r4 preparation validation — BLOCKED

Date: 2026-08-06

Status: `BLOCKED`; M3 remains `IN_PROGRESS`; M4 and M5 remain `NOT_STARTED`.

## Boundary

- Branch: `codex/m3.1.1-r4-acceptance-repair`
- Immutable r3 baseline: `dbcbbb6b4fe501de87f655edf22099ad62363c57`
- HEAD used for the failed byte gate: `c3ec32a`
- No r4 fresh context was launched, created, or communicated with.
- No r4 prompt, acceptance manifest, model final, payload, bundle, outcome, one-shot receipt, or context record was created.

## Preserved failure

The preparation stopped at the required immutable-byte audit:

```text
python -X utf8 evals/m3/audit_immutable_forward_evidence.py evals/m3/results/forward-r4/r2-r3-byte-preservation.json
exit=1
status=invalid
tracked immutable files audited=66
filesystem/Git-blob mismatches=20
```

The 20 mismatches are the Windows CRLF materialization of historical r2 result/report files whose Git blobs are LF. The r2 input trees and r3 trees match their Git blobs. The audit records both filesystem raw SHA-256 values and Git blob object/raw hashes in `evals/m3/results/forward-r4/r2-r3-byte-preservation.json`; no historical file or blob was edited. Path-specific LF rules were added for future materialization, but applying them to the existing checkout would overwrite immutable evidence and is prohibited.

## Completed before the block

- The r4 implementation plan was committed separately at `e9fed02`.
- The closed model-output contract and field-level composer diagnostics are implemented and focused tests pass: `16/16`.
- The manifest-owned dispatcher is implemented and focused tests pass: `7/7`.
- The preparation and byte-audit unit tests pass: `6/6`; the byte-audit test intentionally requires the checkout mismatch to remain visible rather than relabeling it as preserved.
- The strict M3 validator and r2/r3 evidence were not weakened or relabeled.

## Later gates not run

The following remain `NOT_RUN` because the immutable byte gate is blocked:

- r4 input generation and five-case eligibility audit;
- r4 contract/prompt freeze and `prompts_frozen=true` acceptance manifest;
- full unit suite;
- unchanged M1, M2, and M3 deterministic replays;
- package audit, Skill validator, and final scope audit;
- r4 readiness status update;
- any fresh-context authorization request or consumption.

No research route, experiment, simulation, training, inference, download, service, deployment, upload, or external/model call occurred.
