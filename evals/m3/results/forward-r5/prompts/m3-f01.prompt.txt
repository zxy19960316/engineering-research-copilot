You are the one-shot fresh-context model for m3-f01. Read only AGENTS.md, skills/engineering-research-copilot/SKILL.md, skills/engineering-research-copilot/references/core-method-coaching.md, skills/engineering-research-copilot/references/method-data-ml-hybrid.md, evals/m3/forward-inputs-r5/m3-model-output-contract.schema.json, this prompt, and evals/m3/forward-inputs-r2/m3-f01-bounded-confirmed.bundle.json. Do not read tests, fixtures, validators, composers, manifests, historical results, or other cases. Do not use network access or write files.

Treat the M2 JSON as immutable. Verify that route_output is null and the direction is user-confirmed. Resolve the one selected formal direction and derive its claim types, metric IDs, claim-specific preconditions, resource ceilings, eligible round-two sources, and authoritative minimum-decisive-test stop and pivot criteria. Derive every value from the input; do not trust copied declarations or invent missing values.

Finalize exactly once as one compact JSON object. Read the shared r5 model-output contract. Derive coaching_mode from the immutable input: bounded requires route_output to be null; route_specific requires route_output to be present and compatible. Use metric IDs as strings in primary_metrics; use criterion objects only in stop_conditions and pivot_conditions. Do not read tests, fixtures, validators, manifests, historical results, or other cases.

The object must contain exactly coaching_mode, method_cards, and domain_overlays. Set coaching_mode to bounded and domain_overlays to an empty array. Create at least one closed data_ml_hybrid method card and no other family.

Every card must contain exactly schema_version, card_id, method_family, applicability, assumptions, minimum_resources, inherited_constraints, baselines, controls, procedure_outline, primary_metrics, uncertainty_handling, validation_checks, failure_modes, stop_conditions, pivot_conditions, safety_boundaries, and source_ledger. Use schema_version m3.1. Applicability must contain exactly supported_claim_types, required_inputs, and incompatible_conditions. Keep every required list non-empty except domain_overlays. Copy inherited_constraints exactly, in order and with unchanged number types, from the selected direction. Bind every minimum resource to an inherited < or <= ceiling with the exact constraint ID, resource name, and unit, and keep the required value within that ceiling. Use only selected-direction claim types and metric IDs. Include every metric referenced by a chosen stop or pivot condition in primary_metrics and its claim type in supported_claim_types. Copy numeric stop and pivot objects exactly from the selected direction's authoritative criteria; do not invent or tune thresholds.

Use only recommendation-eligible verified candidates from source_m1_bundle.round2.candidate_pool. Each source row must contain exactly source_id, candidate_id, basis_level, support_types, supports, does_not_support, and limitations. Map metadata_level to metadata, abstract_level to abstract, and fulltext_level to full_text. Metadata rows may support only bibliographic_identity. Keep all narrative lists non-empty. State that generalization, uncertainty quality, and transfer remain unproven until a target-domain decisive test. Do not create a route or claim model performance.

