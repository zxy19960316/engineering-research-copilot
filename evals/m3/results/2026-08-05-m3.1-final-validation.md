# M3.1 Local Final Validation

Date: `2026-08-05`

Status: `LOCAL_IMPLEMENTATION_AND_VALIDATION_COMPLETE; M3_ACCEPTANCE_PENDING`

Validated input HEAD: `4a31fae47d85a1ce70ea944db38fb2ebfa7c4eb6`

Baseline M2.1.1 HEAD: `d0f5e9017044ba35d0ac4559591028228f3b22d8`

Environment: Windows, Python `3.13.5`, branch `codex/m3-engineering-method-cards`.

## Decision

The M3.1 implementation, upstream threshold binding, backward regressions, deterministic offline replay, standard Skill validation, and rendered-link package audit passed locally. All 15 final closure validation commands below passed on their first attempt; none was retried.

M3 remains `IN_PROGRESS` and is not accepted as complete. The required genuinely fresh-context forward evaluation is `NOT_RUN` for the preserved blockers in `2026-08-05-forward-evaluation-not-run.md`. Exact-closure-HEAD GitHub Actions is also `NOT_RUN — push not authorized`. Baseline Actions run `30977286846` validates only the accepted M2.1.1 input HEAD and is not M3 implementation CI.

## Local command record

1. `python -X utf8 -m py_compile skills/engineering-research-copilot/scripts/validate_m3_method_bundle.py evals/m3/build_fixtures.py evals/m3/replay_offline_results.py evals/m3/audit_skill_package.py`
   - Exit: `0`.
   - Result: all four M3 validator, builder, replay, and tracked package-audit scripts compiled.
2. `python -X utf8 -m unittest discover -s tests -p "test_*.py" -v`
   - Exit: `0`.
   - Result: `247` tests passed in `13.489s`.
3. `python -X utf8 evals/m1/replay_offline_results.py`
   - Exit: `0`.
   - Result: `valid`; mismatch count `0`.
4. `python -X utf8 evals/m1/replay_machine_artifacts.py`
   - Exit: `0`.
   - Result: `valid`; Case A `valid`, preserved Case B `evidence_incomplete`, Case C `valid`; mismatch count `0`.
5. `python -X utf8 evals/m2/build_fixtures.py`
   - Exit: `0`.
6. `git diff --exit-code -- evals/m2/adversarial-cases.json evals/m2/fixtures`
   - Exit: `0`.
   - Result: exact regeneration diff empty for the manifest and `34` fixtures.
7. `python -X utf8 evals/m2/replay_offline_results.py`
   - Exit: `0`.
   - Result: `valid`; `34/34` cases matched and the frozen record matched.
8. `python -X utf8 evals/m3/build_fixtures.py`
   - Exit: `0`.
9. `git diff --exit-code -- evals/m3/adversarial-cases.json evals/m3/fixtures`
   - Exit: `0`.
   - Result: exact regeneration diff empty for the manifest and `16` fixtures.
10. `python -X utf8 evals/m3/replay_offline_results.py`
    - Exit: `0`.
    - Result: `valid`; `16/16` cases matched and `offline-results.json` matched the frozen record.
11. `python -X utf8 C:\Users\94310\.codex\skills\.system\skill-creator\scripts\quick_validate.py D:\engineering-research-copilot\skills\engineering-research-copilot`
    - Exit: `0`.
    - Result: `Skill is valid!`.
12. `python -X utf8 evals/m3/audit_skill_package.py`
    - Exit: `0`.
    - Exact output: `{"direct_link_count":13,"errors":[],"reference_count":13,"skill_lines":99,"status":"valid"}`.
13. PowerShell CI audit-wiring check for `.github/workflows/m1-validation.yml`.
    - Exit: `0`.
    - Exact output: `CI audit wiring valid: total=2 compile=1 run=1`.
14. `git diff --exit-code 4d679c936caf04e00aa462a55fcb1b1d309ff9e2 -- .github/workflows/m1-validation.yml`
    - Exit: `0`.
    - Result: no CI workflow drift after the tracked audit was wired for compilation and execution.
15. `git diff --check`
    - Exit: `0`.

The P2 red-first command `python -X utf8 -m unittest tests.test_audit_m3_skill_package -v` first exited `1` because `audit_skill_package` was intentionally absent; collection reported `ModuleNotFoundError: No module named 'audit_skill_package'`. After implementation, the same focused suite passed `7/7` with exit `0`. This expected red result is separate from the refreshed closure gates above, all of which passed first attempt.

The final audit-hardening red-first run of that focused command then exited `1` with `12` discovered tests: `4` failures and `1` error exposed non-rendered link-like text being counted, Markdown-named directories and broken/file symlinks being counted as references, and the absent Windows-reparse check. Both symlink cases executed on this Windows host and were not skipped. After commit `4a31fae47d85a1ce70ea944db38fb2ebfa7c4eb6`, the focused suite passed `12/12` in `0.234s` with exit `0` and no skips.

The earlier closure-document staging check remains preserved: its first `git diff --cached --check` reported `evals/m3/results/2026-08-05-m3.1-final-validation.md:78: new blank line at EOF.` The excess final blank line was removed, and the staged check passed on rerun. No failure was relabeled as a pass.

## P1 and P2 commits

- P1 threshold contract: `ced27284ee79fe316b069f041b769c9778f256ef` (`test: bind M3 card conditions upstream`).
- P1 implementation: `badf4a9c62d271d13fec4adbe4f148ad3d028cf0` (`fix: reject invented M3 thresholds`).
- P2 package-audit contract: `9d5f4a0402ca3ed741a37d574feac72bf5b2e400` (`test: specify M3 package audit`).
- P2 audit and CI enforcement: `4d679c936caf04e00aa462a55fcb1b1d309ff9e2` (`ci: enforce M3 package links`).
- P2 rendered-link and filesystem-entry contract: `cb9e4a921b6b206fb9e146fbe70e727c112b276b` (`test: reject non-rendered M3 reference links`).
- P2 rendered-link and safe-reference implementation: `4a31fae47d85a1ce70ea944db38fb2ebfa7c4eb6` (`fix: audit rendered M3 reference links`).

## Frozen evidence hashes

- `evals/m3/adversarial-cases.json` SHA-256: `0830cc166940cf2ec5e648ebcf429b2e2c063249b71342f7836fa442a5d8197e` (`4455` bytes).
- `evals/m3/offline-results.json` SHA-256: `9311db0b8889bb29e6a0e0210f4dcc07293693c706ca9c87b2813d6198b6f811` (`7230` bytes).

## CI change

Commit `41fcc030415395b562b5b722ec09f142b354d7b4` (`ci: add M3 deterministic validation`) renamed the workflow to include M3.1, retained every existing M1/M2 compilation, test, replay, and package-audit gate, and added:

- M3 validator, fixture builder, and replay compilation;
- M3 fixture generation;
- exact zero-diff enforcement for `evals/m3/adversarial-cases.json` and `evals/m3/fixtures`;
- frozen M3 replay, which also checks `evals/m3/offline-results.json` without rewriting it.

Commit `4d679c936caf04e00aa462a55fcb1b1d309ff9e2` retains those M1/M2/M3 gates, compiles the tracked package audit, and replaces the inline filename-substring audit with `python evals/m3/audit_skill_package.py`. Commit `4a31fae47d85a1ce70ea944db38fb2ebfa7c4eb6` further limits extraction to rendered inline Markdown links by excluding fenced code, inline code spans, multiline HTML comments, escaped opening brackets, and images. It accepts only readable regular top-level reference files after `lstat` symlink/reparse/type checks and a read probe; external links are not followed. Duplicate, dangling, unlinked, nested, invalid-file, metadata, forbidden-file, and marker failures remain compact closed JSON with exit `0/1`.

No push, merge, remote configuration, service startup, model or data download, route execution, experiment, simulation, training, upload, or deployment occurred.

## Evidence class and limits

This is local deterministic validation and offline contract replay. It establishes that the checked code and frozen fixtures satisfy their structural contracts at the validated input HEAD. It does not establish fresh-context method-coaching behavior, real citation accuracy, empirical experimental or simulation performance, model performance, target-domain transfer, operational readiness, or nuclear safety.

## Pending acceptance gates

- [x] Local M3 compilation, full regression suite, M1/M2 replay, 16-case M3 deterministic regeneration/replay, standard Skill validation, tracked exact-link package audit, and diff check pass.
- [ ] Genuinely fresh-context M3 forward evaluation: `NOT_RUN` because independent accepted upstream inputs are unavailable; see `2026-08-05-forward-evaluation-not-run.md`.
- [ ] Exact-closure-HEAD GitHub Actions: `NOT_RUN — push not authorized`.
