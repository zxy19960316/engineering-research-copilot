# M3.1.1 r3 acceptance architecture validation

Date: `2026-08-06`

Validated implementation/evidence HEAD: `0ba619a107f6616083a9531c2770ea7e0908e2af`

Status: `ARCHITECTURE_VALID; ACCEPTANCE_NOT_RUN`

This record validates only the deterministic r3 acceptance architecture and reusable input eligibility. It is not a fresh-context result, an M3 closure result, or permission to execute a route, experiment, simulation, model, service, deployment, or operational action.

## Preserved r2 evidence

- Frozen r2 HEAD: `6ec576a47a804300e2a95e6bddfff1d00c5270a3`.
- Freeze record: `evals/m3/results/forward-r3/r2-freeze-diagnostic.json`.
- Existing r2 file count bound by raw SHA-256 and Git blob identity: `27`.
- F01 output remains UTF-8 but JSON-invalid at line `1`, column `20020`, position `20019`.
- F05 remains a one-shot case with no saved output file.
- No r2 file was overwritten, normalized, retried, deleted, or relabeled.

## R3 architecture

- Deterministic composer uses exactly three positional paths, validates immutable M2, recomputes source/selected hashes, deep-copies the eight-field envelope, rejects unknown or incomplete payloads, and writes UTF-8/no-BOM JSON atomically.
- Case-level eligibility closes each input as `eligible`, `not_run`, or `contract_conflict` before prompt freezing.
- F03 forward outcome is modeled as `accepted_expected_block`; it is never treated as a valid M3 method bundle.
- The r3 one-shot outcome runner records exact argv, validator script SHA-256, exit code, stdout SHA-256, invocation count, and distinct UTF-8 versus JSON syntax diagnostics without repairing artifacts.
- The successful M3 method-bundle validator remains strict and unchanged.

## Reusable input audit

| Case | R3 status | Mode | Input decision |
| --- | --- | --- | --- |
| F01 | `eligible` | `bounded` | Reuse immutable route-free r2 M2 input. |
| F02 | `eligible` | `route_specific` | Reuse immutable route-compatible r2 M2 input. |
| F03 | `eligible` | not applicable | Reuse immutable non-empty approved-change r2 M2 input; expect exact block. |
| F04 | `not_run` | `bounded` | No independently accepted complete non-nuclear M1/M2 input exists. |
| F05 | `eligible` | `route_specific` | Reuse F02 route-compatible lineage; do not reuse the old route-free r2 F05 input. |

F05 additionally requires claim-limited non-preprint safety support, specialist review, no operational or safety credit, and `transfer_status: hypothesis`.

R3 preparation manifest status is `blocked_by_f04_upstream_input`. `fresh_contexts_consumed` is `0`; `prompts_frozen` is `false`.

## Local deterministic validation

- Python compilation: exit `0`.
- Unit tests: `314` passed.
- M1 frozen replay: `valid`, zero mismatches.
- M1 machine replay: Case A `valid`, Case B preserved `evidence_incomplete`, Case C `valid`, zero mismatches.
- M2 fixture regeneration: clean.
- M2 replay: `34/34`, all matched.
- M3 fixture regeneration: clean.
- M3 replay: `20/20`, all matched.
- Package audit: `valid`; root Skill `99` lines, `13` references, `13` direct links.
- Standard Skill validator under Python UTF-8 mode: `Skill is valid!`.
- `git diff --check`: clean.
- Worktree after validation: clean.

## Blocking gate

F04 must be produced by a separate upstream evidence context that has not read M3 validator/tests/fixtures or prior M3 outputs. It must create a new non-nuclear `M1_COMPLETE`, a valid provisional `m2.1.1` portfolio, obtain explicit user confirmation for the exact formal direction, then receive an independent acceptance receipt and case eligibility result. The incomplete historical bearing Case B cannot be repaired or promoted for this purpose.

Until that gate is met, do not freeze r3 prompts, consume fresh contexts, run case validators, create a closure candidate, push, or mark M3 complete.
