# M3.1.1 r5.2-f02 Gate 2 Protocol Preparation Validation

Date: `2026-08-07`

Branch: `codex/m3.1.1-r5.2-f02-root-cause-and-protocol-preparation`

Preparation baseline: `263af3df0d8c075d4cdd9835eabe0708dc4f4163`

Implementation HEAD before this evidence record: `1b6583b694c0d2263bcf7e12bf62b4a5e6567a47`

## Outcome

Gate 2 is locally ready for exact-HEAD CI. This is protocol-preparation evidence only. It creates no fresh task, consumes no model finalization, authorizes no execution, and does not enter Gate 3.

The frozen state is:

- revision: `r5.2-f02`;
- case: `m3-f02`;
- result root: `evals/m3/results/forward-r5.2-f02/`;
- logical result artifacts: `0` (`.gitkeep` is an empty tracking marker only);
- task/finalization/composer/validator/retry counters: `0/0/0/0/0`;
- `new_fresh_run_authorized=false`;
- execution-authorization receipt instance: absent;
- reserved task ID: `null`;
- fresh execution: `NOT_RUN`;
- cross-revision aggregate: `NOT_RUN`;
- M3 closure: `NOT_RUN`;
- M4: `NOT_STARTED`.

## Authorization and model-visible context

The process authorization layer, model-visible task, and external operator authorization are separate artifacts. The frozen prompt starts with:

```text
This is the authorized r5.2-f02 execution.
Execute the frozen task now.
```

The prefix is included in prompt SHA-256 `815eae213701505755fb7edc4d64d16089bd4e14e14dc6ec1e16c787918ea1df`. Prompt lint passes with no case-insensitive occurrence of:

- `do not execute`;
- `future task`;
- `without separate authorization`.

The future five-field Gate 3 receipt schema binds:

- `revision = r5.2-f02`;
- `authorized = true`;
- the frozen prompt SHA-256 above;
- input-binding SHA-256 `3d90ed7f02a865eb3cab0fd8f70f0407ce5a80a93e500996686e2fad54c1709d`;
- `authorized_task_count = 1`.

No receipt instance is present. The Gate 2 dispatcher discards its injected callback and rejects any attempted Gate 3 receipt consumption.

## Output boundary and capability decision

Official model documentation records native Structured Outputs support for GPT-5.6 Sol. The current `codex_app.create_thread` request contract observed on `2026-08-07` exposes only `model`, `prompt`, `target`, `thinking`, and `title`; it exposes no response-format or JSON-Schema request field. The frozen decision is therefore:

- selected mode: `strict_text_json_fail_closed`;
- structured-output request config: `null`;
- automatic repair: forbidden;
- direct API substitution: not authorized;
- execution-surface capability recheck before Gate 3: required.

The strict parser accepts exactly one UTF-8 JSON object and requires the first and last non-whitespace bytes to be `{` and `}`. It rejects a UTF-8 BOM, Markdown fences, leading or trailing prose, comments, duplicate keys, multiple JSON values, non-object roots, invalid UTF-8, non-finite numbers, truncation, and empty output. It never repairs or retries.

The nine frozen synthetic cases cover a valid object, fenced object, leading prose, truncation, BOM, duplicate keys, empty output, authorization-refusal prose, and valid JSON rejected by the schema validator. A valid object invokes the validator exactly once; malformed JSON invokes it zero times; every synthetic retry count is zero.

## Raw response observation

The pre-parser schema requires preservation of:

- raw response bytes, SHA-256, and byte length;
- model, task, request, and finalization identities;
- finish reason;
- input and output token counts;
- task creation and completion times;
- request-envelope SHA-256;
- model-visible-messages SHA-256.

Provider fields that the execution surface does not expose must be recorded as `null` with explicit `not_exposed` status. They must not be inferred. This observation layer is not a result repair mechanism.

## Local validation

- Python compilation: passed for the protocol, preparation auditor, and callback-free dispatcher.
- Focused Gate 2 tests: `32/32` passed.
- Combined Gate 1/Gate 2 regression tests: `49/49` passed.
- Full unit suite: `518/518` passed in `393.543s` on the final rerun.
- M1 offline replay: `valid`; mismatches `[]`.
- M1 machine-artifact replay: Case A `valid`, Case B `evidence_incomplete`, Case C `valid`; mismatches `[]`.
- M2 frozen replay: `34/34`, all matched; regeneration diff empty.
- M3 frozen replay: `20/20`, all matched; regeneration diff empty.
- package audit: `valid`; 99 Skill lines, 13 direct links, 13 references, errors `[]`.
- standard Skill validator: `Skill is valid!`.
- r5 CI-state audit: `valid`; historical r5 acceptance remains `blocked_not_accepted`.
- r5.1-f02 terminal audit: `terminal_not_accepted`; accepted `false`; composer `1`; validator `0`; retry `0`.
- Gate 1 root-cause audit: `root_cause_confirmed`; logical-empty Gate 2 root safe; no fresh execution authority.
- Gate 2 preparation audit: `gate_2_preparation_valid`; prompt lint errors `[]`; logical result artifact count `0`; historical r5/r5.1 diffs `empty`.
- Gate 2 dispatcher preflight: `gate_2_preparation_ready`; callback invocations `0`; side effects `[]`.
- historical r5 Git tree matches evidence HEAD `1b696bce53ee0a11163bfe4f91a9a49ab3af6f49`.
- historical r5.1-f02 Git tree matches evidence HEAD `fb5eec44bbf86446cf12bda2bddc76fcb07a7e69`.

The first full run exposed one stale Gate 1 assumption that the successor result root must remain absent forever. Gate 2 legitimately adds only an empty `.gitkeep`. The historical auditor was updated in an independent commit to accept either the original absent state or this logical-empty marker while rejecting every real result artifact. The affected 49 tests and final 518-test suite then passed.

## Remote gate

Exact-HEAD GitHub Actions: `PENDING` at this record commit. It must pass on the final pushed HEAD before Gate 2 exits. This record intentionally does not pre-claim its own remote run or SHA.

No pull request, merge, tag, Gate 3 authorization, fresh task, fresh finalization, cross-revision aggregation, M3 closure, or M4 work is authorized by this record.
