# M2.1.1 Final Validation

Date: `2026-08-05`

Status: `LOCAL_PASS_REMOTE_CI_PENDING`

Acceptance input HEAD: `f0a44890fb9e3244ad86fb60f01065715ddb4de0`

Local implementation acceptance HEAD: `b66bb5122c11730377ee587d59df3717a37e90ba`

The commit containing this record and the matching `STATUS.md` transition cannot embed its own SHA. The final local HEAD is reported after that documentation-only commit. M2.1.1 is not `COMPLETE` because its final exact-HEAD GitHub Actions run requires an unauthorized push.

## Scope

M2.1.1 hardens M2 confirmation provenance, direction-to-route binding, bounded decisive tests, preprint support policy, core-claim coverage, data preconditions, direction axes, and scorecard rationales. It does not start M3, execute an experiment or simulation, train or download a model, start or deploy a service, allocate large resources, connect RRC, or add platform integration.

## Local gates

| Gate | Result |
|---|---|
| Python compilation | exit `0` |
| Full unit discovery | 163 passed, exit `0` |
| M1 frozen fixture replay | `valid`, zero mismatches |
| M1 machine-artifact replay | Case A `valid`, Case B `evidence_incomplete`, Case C blocking audit `valid`, zero mismatches |
| M2.1.1 fixture generation | exit `0` |
| Fixture regeneration diff | empty |
| M2.1.1 adversarial replay | `valid`, 34/34 matched frozen expectations |
| Standard Skill validator | exit `0`, `Skill is valid!` |
| Repository package audit | pass; explicitly not the standard Skill validator |
| Git diff check | clean before this record |
| Worktree | clean before this record |

## Package audit

- Root Skill length: 108 lines, below 500.
- Root-linked references: five.
- Missing or unlinked references: zero.
- Unresolved `TODO`, `TBD`, `[TODO]`, or `PLACEHOLDER` markers: zero.
- Forbidden README, changelog, or installation-guide files in the Skill: zero.
- M2 validator dependencies: Python standard library plus the local M1 validator.
- Network-provider, model, cache, paper-corpus, service, deployment, RRC, platform, and M3 additions: zero.

## Contract acceptance

- `m2.1.1` requires all new safety fields; an m2.1 bundle is not silently accepted.
- Canonical hashing retains UTF-8, sorted keys, compact separators, preserved non-ASCII text, and non-finite-number rejection.
- `user_confirmed` requires a closed user-role event, explicit formal direction ID in the exact source excerpt, exact excerpt SHA-256, and canonical pre-confirmation bundle hash.
- Non-confirmed states cannot carry a confirmation event, and high-risk or unknown IDs cannot be confirmed.
- Route output binds the exact selected direction, confirmation event, and confirmed bundle with recomputed canonical hashes.
- Copied D2/D3 content cannot pass as D1 through ID relabeling alone.
- Route claims, metrics, data preconditions, numeric Go/Stop/Pivot conditions, and inherited resource limits trace to the selected direction and minimum decisive test.
- Minimum decisive tests use exactly two to four closed bounded steps, finite numeric claim criteria, closed preconditions, and size limits; route-shaped nesting and oversized plans fail.
- `provisional_main` and passing safety gates cannot rely solely on `verified_preprint`; blocked or ineligible non-preprints cannot bypass the rule.
- Every core claim uses legal M1 IDs and typed metrics; uncertainty and open-set claims require corresponding uncertainty and open-set metrics.
- Unresolved blocking preconditions cannot coexist with a passing hard gate. Failed gates force a null scorecard, excluded recommendation, and `evidence_incomplete` portfolio and decision state.
- Direction axes are derived against the provisional-main `axis_profile`; main, adjacent, and transfer directions require 0, exactly 1, and at least 2 differences.
- Exact duplicate normalized score rationale triples are rejected; all eight Case A dimension rationales are distinct.

## Forward evidence

### Case A

- The original m2.1 Case A artifact remains unchanged as historical evidence.
- Revised artifact: `2026-08-05-case-a.m2.1.1.bundle.json`.
- Independent fresh-context result: `valid`, zero errors and zero gaps, one finalized validator invocation.
- D1 has separate predictive-performance, uncertainty-quality, and data-availability claims with numeric criteria.
- Manifest and leak-safe split preconditions are `bounded_testable`, block fitting until checked, and include numeric stop conditions for sample count and partition overlap.

### Cases B-E

- The 13 pre-existing Case A-E artifacts remain byte-for-byte unchanged.
- Historical fresh-context Case B remains `valid` for the transfer-boundary audit.
- Historical Case C remains an honest M1 `evidence_incomplete` stop with no M2 portfolio or route.
- Historical Case D remains `valid` after refusing an unconfirmed route request; m2.1.1 additionally freezes `route-before-confirmation` as an invalid adversarial case.
- Historical Case E remains `valid` for the earlier gate-open transition. Its old m2.1 state did not contain m2.1.1 confirmation provenance; the new Case F confirmed artifact supplies and validates that required event without rewriting Case E.

### Case F

- Independent fresh-context route result: `valid`, zero errors and zero gaps, one finalized validator invocation.
- Pre-confirmation canonical hash: `414752adaf6d946f84baac05dab017f5ab848d87047667903c3a23d676db43b5`.
- D1 direction hash: `0f92190cf923e2940e18b2a4ecffd6ebe62c6ee5b3e0bde044b92dbd8b88d1fe`.
- Confirmation-event hash: `c38b945289d48248730e23c64d6bd469fa37241a75762e667a2f6a7e694e1242`.
- Confirmed-bundle hash: `1be3084ad30397f07823182124df98053c0ef6de413c81eafbac3d86f0b81969`.
- Independent read-only recomputation matched all four hashes.
- Data manifest and split preflights precede any fitting; the route inherits one-GPU, 24 GiB, 14-day, and two-fit ceilings with no approved changes.
- No route step was executed.

## Remote state

- Accepted M2 input HEAD `f0a44890fb9e3244ad86fb60f01065715ddb4de0`: GitHub Actions run `30972309423`, job `92199125940`, conclusion `success`.
- M2.1.1 implementation HEAD: exact-HEAD GitHub Actions `NOT_RUN` because push was not authorized and the branch remains local.
- The updated workflow includes compile, full unittests, both M1 replays, M2.1.1 generation plus zero-diff enforcement, M2.1.1 frozen replay, and a repository package audit explicitly labeled as not the standard Skill validator.
- The standard Skill validator remains a separate successful local gate because no reproducible official runner entry is available inside this repository.

## Evidence boundary and remaining limitation

Offline unit and fixture results prove deterministic contract behavior only. Fresh-context Cases A and F prove workflow behavior on frozen artifacts only. None proves empirical direction merit, target-domain transfer success, experiment or simulation performance, operational nuclear safety, host-system user identity, or deployment readiness.

The only closing blocker is final exact-HEAD remote CI. Do not mark M2.1.1 `COMPLETE`, merge it, or begin M3 until the branch is pushed under explicit authorization and that exact HEAD passes the updated workflow.
