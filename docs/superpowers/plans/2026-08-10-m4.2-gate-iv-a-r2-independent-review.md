# M4.2 Gate IV-A r2 Independent Review Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Independently review the repaired M4.2 preparation at exact HEAD `44d1004da1cbb2681ee0d423d1748f98fbaa13e4`, preserve the old blocked Gate IV-A as immutable evidence, and approve only later Gate IV-B protocol-proof preparation after review push and PR CI are truly green.

**Architecture:** Treat repair commit `44d1004...` and tree `9845b1a05e23fa84e55ad20399ec1b86bc861e44` as the immutable reviewed object graph. Create new r2-named schema, artifact, auditor, tests, and CI jobs; bind `.gitattributes` and all repair artifacts directly from Git blobs; reference the old blocked review at `ac6cc707...` without copying or rewriting its JSON. Use a two-commit review lifecycle: a provisional local-pass artifact must remain pending exact-head CI, then a closure commit may record the first r2 push/PR CI and emit the final protocol-proof-only decision; the closure exact HEAD must itself pass push and PR CI before operational acceptance.

**Tech Stack:** Python 3.13 standard library, closed JSON Schema Draft 2020-12, `unittest`, Git object plumbing, GitHub Actions, Bash strict mode, Windows PowerShell 5.1, PowerShell 7, UTF-8/LF.

## Global Constraints

- Base branch `codex/m4-cross-engineering-forward-evaluation-m4.2-gate-iv-a-r2` exactly on repair HEAD `44d1004da1cbb2681ee0d423d1748f98fbaa13e4` and tree `9845b1a05e23fa84e55ad20399ec1b86bc861e44`.
- Keep repair PR #5, repair branch, PR #4, and old Gate IV-A branch unchanged.
- Preserve old blocked review HEAD `ac6cc70714a90f73b4de09eaf0e521e699296890`, tree `3ee67b0c5ffa53fc2676381e9ab9b79499e2cf6e`, artifact blob `9af5710dfba68ee5038f0c9e832591181d41835e`, and artifact raw SHA-256 `cd68d10a140606d4f7dd0ee6d09ebe49c1b4566aa54345cfe91728eaac06b373`.
- Never change `evals/m4/authorization/m4.2/gate-iv-a-review.json`; create only r2-named review files.
- Include `.gitattributes` in the reviewed artifact set and verify `/evals/m3/results/diagnostics-r5.1/** text eol=lf` plus exact worktree/blob equality for every tracked diagnostics file.
- Verify exactly 60 tasks, six batches, 60 unique request bindings, blind IDs `M4-J121..M4-J180`, zero reused task IDs, direct lineage M4.1, root lineage M4.0, all-false authority, and all-zero counters.
- Preserve M4.1 terminal evidence, results `NOT_RUN`, future execution paths absent, and every historical M3 evidence blob unchanged.
- Repair push run `31354780589` and repair PR run `31354802277` must each be seven of seven successful jobs at `44d1004...`; raw `gh run view --log` marker counts for `FAIL:`, `FAILED (`, `Traceback`, and `##[error]` must all be zero.
- The provisional r2 artifact may not emit approval; it must use `PENDING_M4_2_GATE_IV_A_R2_EXACT_HEAD_CI` and `M4_2_GATE_IV_A_R2_LOCAL_REVIEW_PASSED_PENDING_EXACT_HEAD_CI`.
- Emit `APPROVE_M4_2_GATE_IV_B_PROTOCOL_PROOF_ONLY` and `M4_2_GATE_IV_A_REVIEW_PASSED_NOT_AUTHORIZED` only after provisional r2 push and PR CI are both recorded true green with zero raw-log markers.
- Keep `fresh_execution_authorized=false`, `authorization_created=false`, `execution_created=false`, `claim_created=false`, judge/aggregation/closure `NOT_RUN`, and M5 `NOT_STARTED` in both provisional and final states.
- Stop after r2 closure exact-head push and PR CI. Do not implement Gate IV-B, authorization/control, claim, contexts, tasks, results, judge, aggregation, closure, or M5.

---

## File Map

- `docs/superpowers/plans/2026-08-10-m4.2-gate-iv-a-r2-independent-review.md`: exact r2 scope, provisional/final state machine, evidence sources, publication gates, and stop boundary.
- `evals/m4/authorization/m4.2/gate-iv-a-review-r2.schema.json`: closed provisional/final review contract.
- `evals/m4/authorization/m4.2/gate-iv-a-review-r2.json`: handwritten r2 evidence record; no builder may generate it.
- `evals/m4/authorization/audit_m4_2_gate_iv_a_r2_review.py`: offline, deterministic, read-only Git-object and artifact auditor.
- `tests/test_m4_2_gate_iv_a_r2_review.py`: positive, mutation, EOL, prior-block, CI, permission, repeatability, and side-effect tests.
- `.github/workflows/m1-validation.yml`: compile the r2 auditor and add Ubuntu/Windows r2 review jobs.
- `STATUS.md`: preserve the old blocked review, record repair acceptance, and expose provisional then final r2 state without authority.
- `tests/test_m3_r5_erratum.py`: update only current STATUS assertions required by the r2 lifecycle.

### Task 1: Freeze the r2 Contract Red-First

**Files:**
- Create: `evals/m4/authorization/m4.2/gate-iv-a-review-r2.schema.json`
- Create: `tests/test_m4_2_gate_iv_a_r2_review.py`

**Interfaces:**
- Consumes: immutable repair commit/tree/branch, old blocked review identity, reviewed Git blobs, repair CI evidence, and later provisional r2 delivery CI.
- Produces: `audit_review(repo_root: Path, review_path: Path | None = None, verify_git: bool = True) -> dict[str, object]` with sorted `errors`, zero `reviewer_side_effects`, and either the exact provisional or exact final status.

- [ ] **Step 1: Define the closed root key set**

Require exactly:

```python
ROOT_KEYS = {
    "schema_version", "reviewed_head", "reviewed_tree", "reviewed_branch",
    "reviewed_artifacts", "prior_blocked_review", "repair_evidence",
    "matrix_checks", "identity_checks", "lineage_checks",
    "request_binding_checks", "historical_preservation", "zero_state",
    "lifecycle_requirements", "review_delivery", "findings", "limitations",
    "reviewer_side_effects", "decision", "status",
}
```

Set `additionalProperties: false` at every object level; use exact 40-hex Git IDs, 64-hex hashes, positive run/job IDs, and strict bool-versus-int validation.

- [ ] **Step 2: Freeze the two-state decision rule**

```python
PROVISIONAL = (
    "PENDING_M4_2_GATE_IV_A_R2_EXACT_HEAD_CI",
    "M4_2_GATE_IV_A_R2_LOCAL_REVIEW_PASSED_PENDING_EXACT_HEAD_CI",
)
FINAL = (
    "APPROVE_M4_2_GATE_IV_B_PROTOCOL_PROOF_ONLY",
    "M4_2_GATE_IV_A_REVIEW_PASSED_NOT_AUTHORIZED",
)
```

Require provisional status when `review_delivery.status=PENDING_EXACT_HEAD_CI` and both delivery run records are null. Require final status only when `review_delivery.status=VERIFIED_TRUE_GREEN`, both recorded runs target the same provisional review HEAD, every job succeeded, and all four raw-log marker counts are zero.

- [ ] **Step 3: Specify mutation coverage**

Add one focused mutation for each reviewed HEAD/tree/branch, `.gitattributes` binding, repair changed-path allowlist, prior blocked review identity, task/batch count, reused ID, blind range, lineage, request binding, authority, counter, M4.1 terminal, result state, forbidden path, repair CI run/job/log hash, raw-log marker, provisional/final state mismatch, finding, reviewer side effect, and any execution-authorizing decision.

- [ ] **Step 4: Run red tests**

Run: `python -X utf8 -m unittest tests.test_m4_2_gate_iv_a_r2_review -v`

Expected: fail because the r2 artifact and auditor do not exist.

### Task 2: Handwrite the Provisional r2 Review Artifact

**Files:**
- Create: `evals/m4/authorization/m4.2/gate-iv-a-review-r2.json`

**Interfaces:**
- Consumes: `git show 44d1004:<path>` bytes, old blocked review Git object, M4.2/M4.1 manifests, repair PR #5 metadata, repair CI jobs, and exact `gh run view --log` bytes.
- Produces: canonical UTF-8/LF JSON with `findings=[]`, `reviewer_side_effects=[]`, and pending exact-head CI.

- [ ] **Step 1: Bind the reviewed artifact set**

For each reviewed file record `path`, `git_blob_oid`, `raw_sha256`, and `byte_length`. Include at minimum `.gitattributes`, all four repair paths, M4.2 preparation manifest/schema/builder/auditor/helper/tests/workflow/STATUS, and the M4.1 claim/terminal/terminal auditor.

- [ ] **Step 2: Bind the old blocked review without importing it**

Read `ac6cc707:evals/m4/authorization/m4.2/gate-iv-a-review.json` directly from Git. Record the exact commit/tree/artifact object/hash/length, PR #4, `decision=BLOCKED`, `status=BLOCKED`, three findings, and empty reviewer side effects. Do not add the old artifact to this branch.

- [ ] **Step 3: Independently recompute preparation and repair facts**

Recompute 60 tasks, six batches, IDs J121-J180, identity disjointness, M4.1/M4.0 lineage, and all 60 request hashes from the fixed frame. Require repair diff paths exactly:

```python
{
    ".gitattributes",
    "docs/superpowers/plans/2026-08-10-m4.2-windows-lifecycle-repair.md",
    "evals/m4/audit_m4_2_preparation.py",
    "tests/test_m3_raw_sha_eol_policy.py",
}
```

Require historical M3 commit diff empty, two tracked diagnostics files LF-only and blob-identical, all authority false, counters zero, M4.1 terminal preserved, and future paths absent.

- [ ] **Step 4: Record exact repair CI**

Record seven successful jobs for each repair run, Windows/Ubuntu M4.2 jobs, current lifecycle `655`, focused M4.2 `21`, historical focused `6`, Windows PowerShell 5.1 `60/60`, results `NOT_RUN`, and these raw logs:

```text
push 31354780589: bytes=685943 sha256=6b8bd910c89b5f17510c542f3a2418e20ded2c547d5cef69ff78aa32f78be29d
PR   31354802277: bytes=695517 sha256=5f63879f748cecfc0ca8ddc4ac3b9686fb83aa1fd5683468f22b2e88b0a5a7f2
```

All four banned-pattern counts are zero for both.

- [ ] **Step 5: Freeze provisional state**

Set `review_delivery.status=PENDING_EXACT_HEAD_CI`, null delivery runs, empty findings/side effects, and the exact provisional decision/status. Do not pre-claim r2 CI success.

### Task 3: Implement the Independent Read-Only r2 Auditor

**Files:**
- Create: `evals/m4/authorization/audit_m4_2_gate_iv_a_r2_review.py`
- Modify: `tests/test_m4_2_gate_iv_a_r2_review.py`

**Interfaces:**
- Consumes: the handwritten r2 JSON and immutable Git objects; it never imports either M4.2 builder or preparation auditor.
- Produces: deterministic compact JSON with the artifact's exact provisional/final status only when every check passes.

- [ ] **Step 1: Strict-load and validate the complete key graph**

Reject BOMs, duplicate keys, non-object roots, unknown keys, missing keys, bool/int confusion, malformed hashes, schema disagreement, and invalid provisional/final combinations.

- [ ] **Step 2: Read all trusted inputs from Git**

Use replacement-disabled `git cat-file`, `git show`, `git rev-parse`, `git diff-tree`, and `git merge-base`. Require exact repair head/tree/branch, reviewed artifact blob/hash/length, four-path repair diff, old blocked review identity, and only the planned r2 review paths changed after the repair head.

- [ ] **Step 3: Recompute matrix, identity, lineage, and bindings**

Parse the reviewed M4.2/M4.1 manifest blobs directly and recompute the 60 request hashes. Do not import or execute the preparation implementation.

- [ ] **Step 4: Enforce byte/EOL, history, zero-state, and permission gates**

Require the exact `.gitattributes` rule, diagnostics `text=set/eol=lf`, worktree/blob equality, M3 history unchanged, M4.1 terminal counts/hashes exact, all current/future execution paths absent, all counters zero, and every authorization field false.

- [ ] **Step 5: Enforce repair and review CI state**

Require the recorded repair runs/jobs/log hashes and zero markers exactly. Permit only provisional pending state until delivery evidence is added; permit final protocol-proof-only approval only with verified r2 delivery push/PR records and zero markers.

- [ ] **Step 6: Prove determinism and read-only behavior**

Run the auditor twice, compare stdout bytes, and compare `git status --porcelain --untracked-files=all` before/after. Require no filesystem mutation, network primitive, builder import, or preparation-auditor import.

### Task 4: Add r2 CI and Record Provisional Status

**Files:**
- Modify: `.github/workflows/m1-validation.yml`
- Modify: `STATUS.md`
- Modify: `tests/test_m3_r5_erratum.py`

**Interfaces:**
- Consumes: provisional artifact/auditor/tests and all existing lifecycle jobs.
- Produces: Ubuntu/Windows r2 review jobs while M4.2 remains not authorized.

- [ ] **Step 1: Add compile and two matrix jobs**

Compile the r2 auditor. On Ubuntu and Windows run focused r2 tests, the auditor twice, M4.1 terminal audit, results-not-run audit, forbidden-path assertions, and a clean-worktree assertion. Use Bash strict mode for compound Python commands; run Windows PowerShell 5.1 request checking on Windows and PowerShell 7 on Ubuntu.

- [ ] **Step 2: Record only provisional current state**

Preserve the old blocked review facts and repair CI acceptance. Set active revision to r2 local review pending exact-head CI; keep final approval absent and all authority/execution/result fields false, absent, zero, or `NOT_RUN`.

- [ ] **Step 3: Run the complete local gate**

Require r2 focused tests, auditor repeatability, Windows/Ubuntu current lifecycle, M4.2 focused checks, 60/60 request bindings, M4.1 terminal, results `NOT_RUN`, future paths zero, `git diff --check`, protected-path diff empty, and clean tree after commit/auditors.

### Task 5: Publish Provisional r2 and Close Only After CI

**Files:**
- Stage only the eight paths listed in this plan.

**Interfaces:**
- Consumes: locally accepted provisional review commit.
- Produces: provisional r2 push/PR true-green evidence followed by one closure commit and its own exact-head CI.

- [ ] **Step 1: Commit and publish provisional r2**

Push the r2 branch and create a Draft PR whose base is exactly the repair branch. Keep PR #4 and PR #5 unchanged.

- [ ] **Step 2: Require provisional exact-head true green**

Require every push and PR job successful, Windows and Ubuntu r2 jobs green, current lifecycle full green, and all four raw-log failure markers zero. If any fail, set r2 `BLOCKED`; do not repair the reviewed repair branch and do not enter Gate IV-B.

- [ ] **Step 3: Record delivery evidence and emit the final decision**

Update only the r2 artifact, STATUS, and necessary status assertions. Bind the provisional r2 exact HEAD plus its push/PR run IDs, job IDs, raw-log lengths/hashes, and zero marker counts. Then and only then set:

```text
decision = APPROVE_M4_2_GATE_IV_B_PROTOCOL_PROOF_ONLY
status   = M4_2_GATE_IV_A_REVIEW_PASSED_NOT_AUTHORIZED
```

- [ ] **Step 4: Require closure exact-head true green and stop**

Push the closure commit, wait for its push and PR CI, require every job green and all four markers zero, then stop. Do not start Gate IV-B in this work package.

## Self-Review Checklist

- [ ] The r2 reviewed head/tree is exactly the accepted repair object, not PR #4, PR #5 metadata alone, or a worktree projection.
- [ ] `.gitattributes` and all repair artifacts are in the reviewed artifact set.
- [ ] The old blocked review is referenced by immutable Git object and remains unchanged.
- [ ] Matrix, identities, lineage, request bindings, permissions, counters, terminal evidence, results state, and forbidden paths are independently recomputed.
- [ ] Repair push/PR CI is seven of seven green with raw-log marker counts zero.
- [ ] Provisional state cannot emit final approval before r2 delivery CI.
- [ ] Final approval permits only Gate IV-B protocol-proof preparation and grants no execution authority.
- [ ] r2 push/PR CI and closure push/PR CI are exact-head true green with raw-log marker counts zero.
- [ ] `reviewer_side_effects=[]`, `findings=[]`, results remain `NOT_RUN`, and future paths remain absent.
- [ ] Gate IV-B, authorization, fresh execution, results, judge, aggregation, closure, and M5 have not started.
