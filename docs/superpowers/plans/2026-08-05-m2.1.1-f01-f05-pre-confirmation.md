# M2.1.1 F01/F05 Pre-Confirmation Bundle Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build an independent, validator-consistent M2.1.1 pre-confirmation bundle for a nuclear-engineering × ML formal direction, with closed predictive-performance, uncertainty-quality, and data-availability claims, then stop at the user-confirmation gate.

**Architecture:** Preserve the accepted M1.2 source bundle byte-semantically inside a newly constructed M2 bundle. Rebuild the three required formal direction cards with new IDs, complete eligible-source and target-domain precondition links, and numeric success/stop/pivot criteria for every selected metric. Keep the decision state waiting for confirmation and `route_output` null; use the frozen M2 validator only as a structural contract check.

**Tech Stack:** JSON, Markdown, PowerShell, Python 3 standard library, and the repository's M2.1.1 validator.

## Global Constraints

- Work only in `D:\engineering-research-copilot-m3-forward-r2`; baseline must remain `d0f5e9017044ba35d0ac4559591028228f3b22d8`.
- Read only M1/M2 contracts, accepted M1 artifacts, M2 validators, `evals/m2/forward-cases.md`, and accepted M2 Case F artifacts.
- Do not read or call M3 validators, M3 tests, `evals/m3`, M3 fixtures, old M3 fresh outputs, or M3 implementation history.
- Do not search or add papers, perform route/experiment/training/download/service/resource work, fabricate citations, or create user confirmation.
- Use new formal direction IDs and a new pre-confirmation bundle hash; do not copy any Case F confirmation event or set `user_confirmed`.
- Save the bundle, raw validator receipt, manifest draft, and construction notes; stop at `MANDATORY_USER_CONFIRMATION_REQUIRED`.

---

### Task 1: Construct the independent pre-confirmation bundle

**Files:**
- Create: `evals/m2/results/2026-08-05-f01-f05-nuclear-ml-pre-confirmation.bundle.json`
- Create: `evals/m2/results/2026-08-05-f01-f05-nuclear-ml-pre-confirmation.manifest.json`
- Create: `evals/m2/results/2026-08-05-f01-f05-nuclear-ml-pre-confirmation.construction.md`

**Interfaces:**
- Consumes: the embedded accepted M1.2 source and eligible candidate IDs from accepted Case F pre-confirmation artifacts.
- Produces: a new `m2.1.1` root object with exactly three formal directions, all required claims/preconditions/limits, `direction_decision.status = waiting_for_user_confirmation`, `confirmation_event = null`, and `route_output = null`.

- [ ] Preserve the complete accepted M1 bundle under `source_m1_bundle`; recompute `source_m1_bundle_hash` using canonical UTF-8 JSON with sorted keys and compact separators.
- [ ] Use fresh direction IDs `NEML-F01-D1`, `NEML-F01-D2`, and `NEML-F01-D3`; keep the required positions `provisional_main`, `adjacent_alternative`, and `transfer_exploration`.
- [ ] Make the main card explicitly nuclear-engineering × ML and include `predictive_performance`, `uncertainty_quality`, and `data_availability` core claims with role-matched metrics and only eligible M1 candidate IDs.
- [ ] Derive the adjacent card from the main axis profile with exactly one changed axis, and the transfer card with at least two changed axes; do not use title-only differences.
- [ ] Give every formal direction complete finite resource ceilings, target-domain data preconditions, eligible supporting/counter sources, and two-to-four bounded minimum-test steps.
- [ ] For every metric in every `claim_coverage` entry, include finite numeric `success`, `stop`, and `pivot` criteria with explicit units; make the data claim depend on explicit preconditions.
- [ ] Keep all route-like execution content absent, set the decision to waiting, and record the exact confirmation reply required by the user without creating a confirmation event.

### Task 2: Run the M2 validator and preserve its raw result

**Files:**
- Create: `evals/m2/results/2026-08-05-f01-f05-nuclear-ml-pre-confirmation.validation.json`

**Interfaces:**
- Consumes: `evals/m2/results/2026-08-05-f01-f05-nuclear-ml-pre-confirmation.bundle.json`.
- Produces: one exact compact JSON validator output with no hand-edited status, errors, or evidence gaps.

- [ ] Invoke only `skills/engineering-research-copilot/scripts/validate_m2_direction_bundle.py` against the new bundle.
- [ ] Preserve the validator's original stdout and map the process status separately; do not repair/retry the bundle based on hidden M3 artifacts.
- [ ] Treat `valid` as contract consistency only; it must not be described as empirical success, target-domain validation, or nuclear-safety validation.

### Task 3: Audit the confirmation gate and stop

**Files:**
- Modify: none beyond the four artifacts above.

**Interfaces:**
- Consumes: bundle, manifest, construction notes, validation receipt, and independently recomputed hashes.
- Produces: final handoff containing file paths, SHA-256 values, validator result, new formal direction ID, claims, limits, decisive-test summary, and the exact confirmation request.

- [ ] Recompute the full bundle SHA-256 from the saved bytes and record it in the manifest/notes without changing the bundle afterward.
- [ ] Verify that no `confirmation_event` exists, no selected direction is set, no route output exists, and no later F02/F03/F05 action occurred.
- [ ] Emit `MANDATORY_USER_CONFIRMATION_REQUIRED` and require the exact reply `我确认正式方向<NEML-F01-D1>。`.
- [ ] Stop immediately after this gate; do not claim F01/F05 accepted.
