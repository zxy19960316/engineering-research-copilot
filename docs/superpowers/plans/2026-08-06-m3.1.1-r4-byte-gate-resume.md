# M3.1.1 R4 Byte-Gate Resume and Fresh Acceptance Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use inline execution with the repository's explicit r4 gates. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Starting from `64706d9a1adb1289bb5d06b51d6fa8ce45066387`, prove the committed LF rules in a genuinely fresh worktree, finish and freeze r4 readiness, then consume exactly five isolated one-shot fresh contexts if and only if every preparation gate is green.

**Architecture:** Keep all r2/r3 files and the prior blocked r4 receipt immutable. Use the existing manifest-owned dispatcher, composer, validator, and preparation auditor; add only successor r4 evidence and a successor readiness/acceptance record. Treat the byte audit and every preparation gate as hard preconditions, and preserve invalid or blocked outcomes without repair or retry.

**Tech Stack:** Python 3.12 standard library, unittest, strict UTF-8 JSON, SHA-256 raw/Git-blob comparisons, PowerShell, Git, the existing Codex task surface, and the repository's package/Skill validators. No network, model/API provider, service, route, experiment, simulation, training, download, upload, deployment, push, PR, merge, tag, or main-branch change.

## Global Constraints

- Work on branch `codex/m3.1.1-r4-byte-gate-resume` at required starting HEAD `64706d9a1adb1289bb5d06b51d6fa8ce45066387`.
- Require ancestry from immutable r3 baseline `dbcbbb6b4fe501de87f655edf22099ad62363c57`.
- Never overwrite, normalize, relabel, amend, squash, rebase, or delete any path under `evals/m3/forward-inputs-r2`, `evals/m3/results/forward-r2`, `evals/m3/forward-inputs-r3`, or `evals/m3/results/forward-r3`, the two historical forward reports, or the blocked receipt `evals/m3/results/forward-r4/r2-r3-byte-preservation.json`.
- Use only `C:\Users\94310\AppData\Local\Programs\Python\Python312\python.exe -X utf8` for validation.
- Do not create a fresh r4 task until the fresh byte audit, five-case preparation, focused/full/replay/package/Skill gates, and clean committed readiness state all pass.
- Each r4 case receives exactly one fresh task and exactly one final compact JSON answer; no follow-up, repair, retry, re-prompt, or same-context substitution is allowed.
- Use only the manifest-owned dispatcher. F03 must resolve from the manifest to `evals/m3/forward-inputs-r2/m3-f03-approved-change.bundle.json`.
- Keep M3 `IN_PROGRESS` unless governance and all acceptance evidence permit a local closure candidate; keep M4 and M5 `NOT_STARTED`; do not claim exact-HEAD remote CI without push authorization.

---

### Task 1: Pass the fresh-checkout byte gate

**Files:**
- Create: `evals/m3/results/forward-r4/r2-r3-byte-preservation-fresh-worktree.json`
- Preserve: `evals/m3/results/forward-r4/r2-r3-byte-preservation.json`

- [ ] Verify this worktree is new, clean, on `codex/m3.1.1-r4-byte-gate-resume`, at `64706d9a1adb1289bb5d06b51d6fa8ce45066387`, and that the required baseline is an ancestor.
- [ ] Verify committed `.gitattributes` LF rules with `git check-attr`; do not run any checkout normalization.
- [ ] Run `evals/m3/audit_immutable_forward_evidence.py` against the new successor receipt path.
- [ ] Require every audited filesystem byte sequence to equal its exact Git blob. On any mismatch or environmental failure, preserve the successor blocked receipt, record all later gates `NOT_RUN`, and stop.

### Task 2: Complete and freeze r4 preparation

**Files:**
- Create or modify only successor r4 paths under `evals/m3/forward-inputs-r4`, `evals/m3/results/forward-r4`, and `evals/m3/forward-cases-r4.md` as required by the existing r4 plan.
- Modify `.gitattributes` only if a missing committed r4 LF rule is proven necessary.

- [ ] Re-run focused contract/composer, dispatcher, preparation, and byte-audit tests with Python 3.12.
- [ ] Generate the five new r4 M2-validation and eligibility receipts from manifest-selected inputs, preserving exact raw/canonical hashes and validator provenance.
- [ ] Write the closed model-output contract, five prompts, case specification, and acceptance manifest without exposing expected results, tests, fixtures, validators, prior cases, or output/receipt paths.
- [ ] Require F01-F05 eligible, all source/prompt/contract hashes exact, future output/receipt/context paths absent, `prompts_frozen=true`, and `fresh_contexts_consumed=0` before freezing.

### Task 3: Commit preparation readiness

**Files:**
- Create: a successor r4 preparation validation record and any successor audit receipts.
- Modify: `STATUS.md` only if all preparation gates are green; preserve M3 `IN_PROGRESS` and M4/M5 `NOT_STARTED`.

- [ ] Run the full unit suite, unchanged M1/M2/M3 deterministic replays, package audit, Skill validator, `git diff --check`, and the r4 preparation auditor.
- [ ] Record exact commands, counts, hashes, branch/HEAD, immutable-byte result, authorization binding, limitations, and all not-run gates.
- [ ] Commit explicit preparation/readiness paths with an independently reviewable commit and leave the worktree clean.

### Task 4: Consume exactly five isolated fresh contexts

**Files:**
- Create only manifest-owned successor r4 result paths under `evals/m3/results/forward-r4/`.

- [ ] Create five genuinely fresh Codex tasks from the exact committed frozen-readiness HEAD, one each for F01-F05, passing only `AGENTS.md`, that case's frozen prompt, and paths whitelisted by that prompt.
- [ ] Do not pass this coordinator history, prior outputs, other cases, tests, validators, fixtures, or expected answer content; independent cases may run concurrently.
- [ ] Save each final answer byte-for-byte and invoke the manifest-owned dispatcher, applicable composer, and one-shot validator at most once.
- [ ] Preserve every invalid JSON, blocked result, missing output, unexpected status, and failed receipt. Never relabel or retry.
- [ ] After all five consumptions, record `fresh_contexts_consumed=5`, exact final/output/receipt hashes, and accepted/invalid status for every case.

### Task 5: Conclude acceptance only from observed evidence

- [ ] If and only if all five outcomes match the frozen acceptance matrix, run the complete local closure suite and record a local closure candidate; do not claim remote CI.
- [ ] If any case or gate fails, preserve it, keep M3 `IN_PROGRESS`, keep M4/M5 `NOT_STARTED`, mark later closure gates `NOT_RUN`, and stop.
- [ ] Commit explicit acceptance/conclusion evidence, verify ancestry and clean state, and do not push or create a PR.

## Self-review checklist

- [ ] The fresh byte receipt is a distinct successor and reports exact filesystem/Git-blob equality for every immutable path.
- [ ] The prior blocked r4 record and all r2/r3 artifacts remain byte-identical and untouched.
- [ ] No r4 fresh task exists before the committed frozen-readiness gate.
- [ ] Exactly five fresh contexts are consumed, exactly once each, with no cross-case leakage or retry.
- [ ] Final claims distinguish accepted, invalid, blocked, replay-only, local, and not-run evidence.
- [ ] The terminal state is either `LOCAL_R4_ACCEPTANCE_GREEN_AWAITING_PUSH_AUTHORIZATION` or `BLOCKED/NOT_ACCEPTED` with later gates explicitly `NOT_RUN`.
