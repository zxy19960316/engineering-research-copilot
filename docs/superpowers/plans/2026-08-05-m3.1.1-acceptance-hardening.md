# M3.1.1 Acceptance Hardening Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Harden the existing M3.1 method-card validator and acceptance evidence so M3 can close only after adversarial replay, independent fresh-context forward evaluation, complete local regression, and exact-closure CI are all honestly recorded.

**Architecture:** Keep the installable Skill and accepted M1/M2 artifacts unchanged except for the narrow validator contract needed by M3.1.1. Derive safety eligibility, metric-to-claim binding, and resource validity from the embedded M2/M1 context; generate deterministic offline fixtures from test builders only for offline contract evidence; keep independent forward inputs and fresh outputs outside the Skill and never treat them as empirical results.

**Tech Stack:** Python 3.12-compatible standard-library scripts, UTF-8 JSON, `unittest`, PowerShell, Git, canonical/raw SHA-256 evidence, and GitHub Actions status checks.

## Global Constraints

- Work from exact HEAD `b0a1b9e41e85a1b57c80a8b571bac9ca01c88778` on `codex/m3.1.1-acceptance-hardening`; do not branch from `main`.
- Keep M4 `NOT_STARTED`; do not implement M4 evaluation logic, route execution, experiments, simulations, training, model downloads, services, deployment, uploads, or resource allocation.
- Preserve all accepted M1/M2 evidence and historical results; do not relabel offline, replay, abstract-only, blocked, or NOT_RUN evidence.
- Use TDD: commit the new red tests before the validator implementation, then commit deterministic fixture changes and acceptance evidence separately.
- Keep all plans, forward inputs, outputs, validations, and acceptance records outside `skills/engineering-research-copilot/`.
- Do not use `rg.exe` if Windows denies execution; use PowerShell file enumeration and `Select-String`.
- Do not push, merge, create a PR, or create the M4 branch without explicit authorization. If push is later authorized, verify the exact closure SHA in Actions before marking M3 complete.

## File Map

- Modify `STATUS.md`: activate M3.1.1, retain the existing M3.1 history, and close M3 only after every required gate passes.
- Modify `skills/engineering-research-copilot/scripts/validate_m3_method_bundle.py`: add the three validator rules while preserving closed, deterministic, read-only behavior.
- Modify `tests/test_validate_m3_method_bundle.py`: add red and positive tests plus fixture-builder helpers that refresh all affected hashes.
- Modify `evals/m3/build_fixtures.py`, `evals/m3/adversarial-cases.json`, `evals/m3/offline-results.json`, and `tests/test_replay_m3_offline_results.py`: extend deterministic adversarial replay from 16 to 20 cases.
- Create `evals/m3/fixtures/preprint-only-general-safety.json`, `negative-minimum-resource.json`, `card-condition-outside-primary-metrics.json`, and `card-primary-metric-claim-mismatch.json` through the deterministic builder.
- Create `evals/m3/forward-inputs/` only from independently prepared M2.1.1 material rooted at `d0f5e9017044ba35d0ac4559591028228f3b22d8`'s accepted parent `d0f5e9017044ba35d0ac4559591028228f3b22d8`; record each raw/canonical hash and M2 validation result.
- Create `evals/m3/results/forward/` only from genuinely fresh case contexts, with one immutable output, one validation result, and one context record per case.
- Create `evals/m3/results/2026-08-05-m3.1.1-final-validation.md` with exact commands, exit codes, counts, hashes, provenance classes, limitations, and CI state.

## Task 1: Activate the acceptance revision

**Files:** `STATUS.md`, this plan.

- [ ] Confirm HEAD, branch, and clean status before editing.
- [ ] Set M3 to `IN_PROGRESS`, acceptance revision to `m3.1.1`, input HEAD to `b0a1b9e41e85a1b57c80a8b571bac9ca01c88778`, record M3.1.1 in progress, and keep M4 `NOT_STARTED`.
- [ ] State that fresh-context forward evaluation and exact-closure CI remain not run until produced; do not claim the baseline M2 Actions run as M3 closure CI.
- [ ] Run `git diff --check` and commit only `STATUS.md` and this plan as `docs: activate M3.1.1 acceptance hardening`.

## Task 2: Freeze red tests for all new validator gates

**Files:** `tests/test_validate_m3_method_bundle.py`.

- [ ] Add a helper that changes one fixture candidate to eligible `verified_preprint`, updates its verification record consistently, and refreshes the M1, M2 confirmation, source-bundle, direction, and M3 hashes.
- [ ] Add `test_general_safety_support_requires_non_preprint` for a `reliability_safety_risk` card whose only safety row is the verified preprint; assert `safety_support_requires_non_preprint_source`.
- [ ] Add a positive test with one verified-preprint safety row and one eligible non-preprint safety row on the same ordinary card; assert the bundle remains valid.
- [ ] Add `test_negative_minimum_resource_is_rejected`; assert `invalid_minimum_resource_required_value` for `required_value = -1`.
- [ ] Add condition/claim binding tests that assert `method_card_condition_metric_not_primary` and `method_card_primary_metric_claim_type_mismatch`.
- [ ] Run `python -X utf8 -m unittest tests.test_validate_m3_method_bundle -v`; preserve the expected red failures before implementation.
- [ ] Commit only the focused test file as `test: specify M3.1.1 safety and card binding gates`.

## Task 3: Implement the three validator gates

**File:** `skills/engineering-research-copilot/scripts/validate_m3_method_bundle.py`.

- [ ] Make `_candidate_is_non_preprint` accept fixture-only candidates but reject a candidate explicitly marked `verified_preprint`, while retaining recommendation eligibility and allowed-status checks.
- [ ] After ordinary card ledger validation, collect card ledger rows whose `support_types` contains `safety`; require at least one such row to resolve to an eligible non-preprint candidate, otherwise emit exactly `safety_support_requires_non_preprint_source`.
- [ ] In `_validate_resource_bounds`, require `required_value` to be finite, non-boolean, numeric, and `>= 0`; emit `invalid_minimum_resource_required_value` for a negative value.
- [ ] Derive `metric_to_claim_type` in the M2 context and require every stop/pivot `metric_id` to be in `primary_metrics`; emit `method_card_condition_metric_not_primary` otherwise.
- [ ] Require each primary metric's upstream claim type to occur in `applicability.supported_claim_types`; emit `method_card_primary_metric_claim_type_mismatch` otherwise.
- [ ] Run the focused suite and the full unittest suite; preserve unrelated historical gates.
- [ ] Commit the implementation as `fix: enforce M3.1.1 safety and internal card bindings`.

## Task 4: Extend deterministic adversarial replay

**Files:** the four fixture files, `evals/m3/build_fixtures.py`, `evals/m3/adversarial-cases.json`, `evals/m3/offline-results.json`, and `tests/test_replay_m3_offline_results.py`.

- [ ] Add four builder-generated cases with exact expected errors: preprint-only general safety, negative resource, condition metric outside primary metrics, and primary metric claim mismatch.
- [ ] Update replay count assertions from 16 to 20 and keep exact manifest/fixture-directory matching.
- [ ] Run builder, replay, byte diff, builder again, and byte diff again; expect deterministic output and a valid frozen replay record.
- [ ] Commit only the deterministic replay changes as `test: freeze M3.1.1 adversarial replay`.

## Task 5: Prepare and audit independent M2.1.1 forward inputs

**Files:** `evals/m3/forward-inputs/*`, plus a dated preparation record outside the Skill.

- [ ] Create a separate worktree from accepted M2.1.1 HEAD `d0f5e9017044ba35d0ac4559591028228f3b22d8`; do not read M3 validator/tests/fixtures/builders there.
- [ ] Preserve five independent inputs and individual M2 validation records. Each manifest row must include case ID, input path, raw/canonical hashes, M2 status/errors/gaps, source M1 artifact/hash, preparation/review contexts, and explicit `does_not_prove` limits.
- [ ] For F01, require confirmed no-route bounded input with complete resources, eligible sources, and upstream numeric stop/pivot criteria.
- [ ] For F02, require an independently accepted route-compatible input with empty approved changes and exact precondition/condition traceability; do not mutate the accepted Case F file.
- [ ] For F03, preserve an independently authored non-empty approved-change input and let M3 fail closed without repairing or retrying it.
- [ ] For F04, use an independently accepted non-nuclear measurement/UQ M1/M2 input. If no complete verifiable input exists, record `M3-F04: NOT_RUN` with the specified reason and keep M3 open.
- [ ] For F05, require user-confirmed nuclear×ML direction, non-preprint safety support, target-domain preconditions, numeric criteria, and `transfer_status: hypothesis`.
- [ ] Commit independently accepted input artifacts as `eval: add independently accepted M3 forward inputs` only when all required input provenance exists; otherwise preserve the explicit blocked/NOT_RUN record and do not claim closure.

## Task 6: Execute fresh-context forward evaluation

**Files:** `evals/m3/results/forward/*`, dated forward-evaluation records.

- [ ] Use one fresh context per case with only the root Skill, named references, immutable input, frozen prompt, and output path. Do not expose validator source, tests, M3 fixtures, builders, expected errors, or prior M3 outputs.
- [ ] Permit one finalization and one validator invocation per case. Do not modify input, repair failed output, retry after failure, or perform any external/research/route side effect.
- [ ] Record context ID, input/prompt/output hashes, references loaded, finalization and validator counts, side effects, deviations, limitations, and exact validator result.
- [ ] Require F01 bounded valid, F02 route-specific valid, F03 exact `unsupported_approved_constraint_change_provenance` fail-closed, F04 valid measurement-only with no nuclear overlay, and F05 valid nuclear overlay with `transfer_status: hypothesis`.
- [ ] Preserve every failure or NOT_RUN case and commit actual results as `eval: record independent M3.1.1 forward evaluation` only when they came from fresh contexts.

## Task 7: Run closure gates and close M3 honestly

**Files:** `evals/m3/results/2026-08-05-m3.1.1-final-validation.md`, `STATUS.md`.

- [ ] Run the requested M1/M2/M3 compilation, full unittest, deterministic replay, package audit, standard Skill validator, `git diff --check`, and status checks under UTF-8 Python.
- [ ] Record local results separately from remote CI. Do not mark M3 complete if any required fresh case is missing or if exact closure SHA CI is not successful.
- [ ] If every gate passes, set `M3: COMPLETE`, record the exact closure SHA, list F01–F05 outcomes, keep `M4: NOT_STARTED`, and commit `docs: close M3.1.1 acceptance`.
- [ ] Do not push or create an M4 branch in this task without explicit authorization; if later authorized, push the exact closure SHA and verify `completed/success` for that same `head_sha` before treating it as accepted.

## Self-review checklist

- Safety coverage applies to every ordinary method card, not only the nuclear overlay, and the safety row itself must bind to the non-preprint candidate.
- Negative, boolean, NaN, infinity, and non-numeric resource values remain invalid; only non-negative finite non-boolean numbers are accepted.
- Conditions and primary metrics are checked against the same card and the upstream claim-type declaration.
- Offline fixture evidence, same-context rehearsal, and structural validation are never called empirical evidence or fresh-context evidence.
- F04 remains an explicit hard gate; an unavailable independent non-nuclear input keeps M3 `IN_PROGRESS`.
