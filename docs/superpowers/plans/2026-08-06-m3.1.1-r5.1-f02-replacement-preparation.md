# M3.1.1 r5.1-f02 Replacement Preparation Plan

> Scope: prepare one replacement F02 revision offline. Do not launch a fresh-context task, compose or consume a result, retry historical F02, aggregate acceptance, close M3, start M4, or push without a separate authorization.

## Baseline and invariants

- Start from accepted r5.1 closure HEAD `c5ca408beedf2c3f20160fb1d06293336eacd725` on a clean worktree.
- Preserve `evals/m3/results/forward-r5/` byte-for-byte from evidence HEAD `1b696bce53ee0a11163bfe4f91a9a49ab3af6f49`.
- Keep M3 `IN_PROGRESS`, r5 `BLOCKED_NOT_ACCEPTED`, M4 `NOT_STARTED`, and `new_fresh_run_authorized=false`.
- Reuse only immutable accepted r5 F01, F03, F04, and F05 evidence. Prepare only a new F02 input binding under revision `r5.1-f02`.
- Reserve the new result root `evals/m3/results/forward-r5.1-f02/`; it must contain zero result/receipt artifacts.

## Task 1: freeze the implementation boundary

**Files**

- Add this plan.
- Update `STATUS.md` only to record the completed corrected r5.1 closure CI and the bounded preparation state.

**Steps**

1. Confirm branch, HEAD, remote baseline, clean tree, r5 evidence diff, and frozen erratum.
2. Commit the plan independently.

## Task 2: strengthen the generic authority contract red-first

**Files**

- Modify `tests/test_validate_m3_method_bundle.py`.
- Add `evals/m3/forward-inputs-r5.1-f02/m3-model-output-contract.schema.json`.
- Add `evals/m3/forward-inputs-r5.1-f02/m3-f02.prompt.txt`.

**Steps**

1. Add a table-driven regression that mutates each authoritative condition field: `metric_id`, `criterion_type`, `value`, and `unit`.
2. Assert every drift produces the existing fail-closed authority error for route-specific method cards.
3. Run the focused test and observe failure before completing any required implementation adjustment.
4. Preserve the validator's general strict-object equality rule; do not add an F02-specific bypass.
5. Freeze a closed output schema whose descriptions state that every stop/pivot criterion is an exact inherited tuple from `route_output` and may not be synthesized, normalized, converted, or rounded.
6. Freeze a replacement prompt that makes stop/pivot authority a pre-output checklist and limits reads/writes exactly as required for one future fresh task.

## Task 3: create the one-case frozen preparation contract

**Files**

- Add `evals/m3/forward-inputs-r5.1-f02/m3-f02.input-binding.json`.
- Add `evals/m3/forward-inputs-r5.1-f02/manifest.json`.
- Add `evals/m3/results/forward-r5.1-f02/.gitkeep`.
- Add `evals/m3/audit_forward_r5_1_f02_preparation.py`.
- Add `tests/test_audit_m3_forward_r5_1_f02_preparation.py`.
- Modify `.github/workflows/m1-validation.yml`.

**Steps**

1. Write failing preparation-auditor tests for the valid frozen state, hash drift, nonzero counters, unauthorized fresh-run state, historical F02 retry/path reuse, nonempty result artifacts, unsafe future paths, and mutation of immutable reused r5 evidence.
2. Bind the existing eligible F02 M2 input by repository path, raw SHA-256, canonical SHA-256, Git blob OID, M2 validation receipt, eligibility receipt, and a canonical hash of authoritative route stop/pivot criteria.
3. Bind the erratum policy and evidence HEAD; record the four reusable accepted cases by their immutable r5 transaction/validation/bundle identities without copying them.
4. Create a one-case manifest with a new revision, new result root, exact prompt/contract/input-binding hashes, canonical future names, all counters zero, and `new_fresh_run_authorized=false`.
5. Implement a read-only fail-closed auditor. It must reject missing or changed bindings, evidence-head drift, old task/output reuse, nonzero counters, unsafe/existing result artifacts, policy drift, and any r5 evidence-tree diff.
6. Treat `.gitkeep` only as a version-control directory marker; report zero result artifacts and reject every other file in the result root.
7. Add compile and preparation-audit steps to CI so an exact-HEAD run cannot skip the new gate.

## Task 4: validate and record preparation readiness

**Files**

- Add `evals/m3/results/2026-08-06-m3.1.1-r5.1-f02-preparation-validation.md`.
- Update `STATUS.md`.

**Steps**

1. Run focused authority and preparation-auditor tests.
2. Run the full unit suite.
3. Run unchanged M1, M2, and M3 replays.
4. Regenerate M2/M3 fixtures and require empty diffs.
5. Run the package audit and standard Skill validator.
6. Run the new preparation auditor and the expected r5 blocked-state auditor.
7. Require `git diff 1b696... -- evals/m3/results/forward-r5` to be empty.
8. Record only local structural readiness. Keep fresh execution, aggregate acceptance, M3 closure, and exact-HEAD remote CI as `NOT_RUN`.
9. Stage explicit paths and create reviewable commits. Stop before push and request separate push authorization.
