# M4.2 Authorization Preparation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Prepare and independently audit the closed M4.2 one-shot authorization schemas and non-executable candidate projections without issuing an authorization, token, claim, task, result, judge call, aggregate, or closure claim.

**Architecture:** Treat Gate IV-B closure HEAD `ad67a79f39685937466d3a49d30c6a5117e2810c` as the immutable successor baseline and bind both its accepted B3 proof HEAD and its B4 closure CI. Add only future-instance schemas plus a preparation-only artifact and read-only auditor. Use a candidate HEAD for implementation acceptance, then one closure commit that records the candidate CI; require the closure HEAD to pass a second external push/PR exact-HEAD gate.

**Tech Stack:** Python 3.13 standard library, JSON Schema Draft 2020-12, `unittest`, Git object plumbing, GitHub Actions, Bash strict mode, PowerShell 7 on Ubuntu, Windows PowerShell 5.1, UTF-8/LF.

## Global Constraints

- Start branch `codex/m4-cross-engineering-forward-evaluation-m4.2-authorization-preparation` exactly at `ad67a79f39685937466d3a49d30c6a5117e2810c`.
- Stack the Draft PR on `codex/m4-cross-engineering-forward-evaluation-m4.2-gate-iv-b-protocol-proof`; do not merge PR #7 and do not rebase from `main`.
- Bind B3 HEAD `249e28d07d5e52cd9cec9b7e110f6159e6046222`, B4 HEAD `ad67a79f39685937466d3a49d30c6a5117e2810c`, and Gate IV-B proof blob `d3fe975431f2e4584a52ee5305b169f5b5d29268`.
- Bind B3 push/PR runs `31370941146` and `31370945548`, each 11/11 successful with zero `FAIL:`, `FAILED (`, `Traceback`, and `##[error]` markers.
- Bind B4 push/PR runs `31371973449` and `31371976651`, each 11/11 successful with zero `FAIL:`, `FAILED (`, `Traceback`, and `##[error]` markers.
- Recompute exactly 12 cases, 5 arms, 60 tasks, 6 batches, 10 tasks per batch, 60 unique request bindings, and all source-byte bindings from immutable inputs.
- Keep whole-matrix authority atomic: partial authority and a second claim are forbidden; a terminal protocol/infrastructure failure requires a successor revision.
- Keep cross-task result visibility, blind-map access, judge execution, unblinding, aggregation, threshold claims, M4 closure, and M5 unavailable.
- Keep the preparation artifact at `PREPARED_NOT_AUTHORIZED`; never emit an executable authorization token or an `AUTHORIZED_UNCONSUMED` preparation state.
- Do not add a writer or `build_m4_2_authorization.py`.
- Never create `evals/m4/authorization/m4.2/execution-authorization.json`, `evals/m4/authorization/m4.2/execution-control.json`, `authorization-token.json`, `acceptance-claim.json`, `evals/m4/execution/m4.2/**`, `evals/m4/results/m4.2/**`, or `evals/m4/results-manifest.json`.
- Keep M4.2 actual counters at zero: authorized tasks, contexts, dispatches, finalizations, observed results, judge scores, retries, repairs, raw model finals, aggregation calls, acceptance claims, and unauthorized side effects.
- Run every auditor twice and require byte-identical stdout; finish every local and CI gate with an empty `git status --porcelain` after committed checkout.
- Do not lower, delete, skip, or weaken an existing test to make this work package pass.

---

## File Map

- Create `tests/test_m4_2_authorization_preparation.py`: red-first absence, schema, projection, recomputation, mutation, read-only, delivery, and workflow contract tests.
- Create `evals/m4/authorization/m4.2/execution-authorization.schema.json`: closed future M4.2 one-shot authorization instance schema only.
- Create `evals/m4/authorization/m4.2/execution-control.schema.json`: closed future M4.2 one-shot control instance schema only.
- Create `evals/m4/authorization/m4.2/authorization-preparation.schema.json`: closed schema for the non-executable preparation artifact.
- Create `evals/m4/authorization/m4.2/authorization-preparation.json`: preparation-only evidence, initially pending candidate exact-HEAD CI and later bound to accepted candidate CI.
- Create `evals/m4/authorization/audit_m4_2_authorization_preparation.py`: deterministic offline auditor plus pure non-instance projection functions.
- Modify `evals/m4/authorization/audit_m4_2_gate_iv_b_protocol_proof.py`: permit only named preparation files while continuing to reject every actual authorization/execution future path and pin the accepted proof artifact to B4.
- Modify `tests/test_m4_2_gate_iv_b_protocol_proof.py`: prove the successor extension preserves the B4 proof blob and the forbidden-state fuse.
- Modify `.github/workflows/m1-validation.yml`: add Ubuntu/Windows preparation jobs and retain exact Gate IV-B replay.
- Modify `STATUS.md`: closure-only final preparation state and candidate CI evidence.
- Modify `tests/test_m3_r5_erratum.py`: closure-only lifecycle assertions without changing historical status.

## Interfaces

- `candidate_authorization_projection(repo_root: Path = REPO_ROOT) -> dict[str, object]` returns a non-instance projection with no token and no executable authority.
- `candidate_control_projection(repo_root: Path = REPO_ROOT) -> dict[str, object]` returns a non-instance control projection with no ready/claim state.
- `audit_authorization_preparation(repo_root: Path = REPO_ROOT, *, artifact_data: Mapping[str, object] | None = None, verify_git: bool = True, present_paths: set[str] | None = None) -> dict[str, object]` recomputes all evidence and returns deterministic status, decision, errors, counts, zero-state fields, and forbidden paths.
- The CLI prints one canonical JSON object with sorted keys and exits `0` only for a valid provisional or final preparation state.

---

### Task 1: AP1 Red-First Successor Contract

**Files:**
- Create: `docs/superpowers/plans/2026-08-10-m4.2-authorization-preparation.md`
- Create: `tests/test_m4_2_authorization_preparation.py`

**Interfaces:**
- Consumes: exact Git objects at B3/B4 and the current M4.2 preparation manifest.
- Produces: executable red tests for every required file and hard absence invariant.

- [ ] **Step 1: Assert exact branch lineage and immutable Gate IV-B evidence**

Add tests that require the current branch baseline to be B4, require B3 and B4 to be ancestors, require proof blob `d3fe975431f2e4584a52ee5305b169f5b5d29268`, and compare the proof raw SHA-256 with `9d160de6893fbb6bd01158524a3a48931496b6d4cae1fdc4c9f0e736921068e0` over 33,204 bytes.

- [ ] **Step 2: Assert all actual authorization and execution paths are absent**

Use this exact hard-fail set:

```python
FORBIDDEN_EXACT = (
    "evals/m4/authorization/m4.2/execution-authorization.json",
    "evals/m4/authorization/m4.2/execution-control.json",
    "evals/m4/authorization/m4.2/authorization-token.json",
    "evals/m4/authorization/m4.2/acceptance-claim.json",
    "evals/m4/results-manifest.json",
)
FORBIDDEN_PREFIXES = (
    "evals/m4/execution/m4.2",
    "evals/m4/results/m4.1",
    "evals/m4/results/m4.2",
    "evals/m5",
)
```

- [ ] **Step 3: Specify red implementation expectations**

Require the two future schemas, preparation schema, preparation artifact, and auditor to exist. Require the artifact not to contain `AUTHORIZED_UNCONSUMED` or a `sha256:` token. Require both projection functions, closed schemas, 12/5/60 recomputation, 60/60 request verification, whole-matrix/single-use constraints, cross-task invisibility, deterministic CLI output, and workflow jobs on both platforms.

- [ ] **Step 4: Run the focused tests and preserve the expected red result**

Run:

```powershell
python -X utf8 -m unittest tests.test_m4_2_authorization_preparation -v
```

Expected: FAIL because the new schemas, artifact, and auditor do not yet exist; no forbidden path is created.

- [ ] **Step 5: Commit the red contract**

```powershell
git add docs/superpowers/plans/2026-08-10-m4.2-authorization-preparation.md tests/test_m4_2_authorization_preparation.py
git commit -m "test(m4): specify M4.2 authorization preparation"
```

---

### Task 2: AP2 Closed Future-Instance Schemas

**Files:**
- Create: `evals/m4/authorization/m4.2/execution-authorization.schema.json`
- Create: `evals/m4/authorization/m4.2/execution-control.schema.json`
- Create: `evals/m4/authorization/m4.2/authorization-preparation.schema.json`
- Test: `tests/test_m4_2_authorization_preparation.py`

**Interfaces:**
- Consumes: M4.1 schema structure and Gate IV-B whole-matrix proof.
- Produces: closed schemas only; no JSON instance and no writer.

- [ ] **Step 1: Define the future execution-authorization schema**

Require exact M4.2 preparation and proof bindings, 60 unique `M4.2-` task IDs, six unique `M4.2-BATCH-` IDs, `whole_matrix_required=true`, `partial_authority_allowed=false`, `second_claim_allowed=false`, one attempt per task, no retry/repair/follow-up, no cross-task results, no judge/blind-map/aggregation/threshold/closure authority, and terminal failure successor policy.

- [ ] **Step 2: Define the future execution-control schema**

Require 60 controlled tasks, six batches, one isolated context/finalization per task, exact allowed context paths, forbidden result/execution context roots, one atomic prelaunch claim, no partial or second claim, and the same negative permissions as the authorization schema.

- [ ] **Step 3: Define the preparation artifact schema**

Use `oneOf` to separate a provisional `PENDING_EXACT_HEAD_CI` delivery with null run records from a final `VERIFIED_TRUE_GREEN` delivery whose push and pull-request records target one accepted candidate HEAD. Close every object with `additionalProperties=false` and require every declared property.

- [ ] **Step 4: Run schema tests and commit**

```powershell
python -X utf8 -m unittest tests.test_m4_2_authorization_preparation -v
git add evals/m4/authorization/m4.2/execution-authorization.schema.json evals/m4/authorization/m4.2/execution-control.schema.json evals/m4/authorization/m4.2/authorization-preparation.schema.json
git commit -m "eval(m4): freeze M4.2 authorization schemas"
```

Expected: remaining failures are limited to the absent preparation artifact/auditor and workflow integration.

---

### Task 3: AP3–AP4 Preparation Artifact, Read-Only Auditor, and Pure Projections

**Files:**
- Create: `evals/m4/authorization/m4.2/authorization-preparation.json`
- Create: `evals/m4/authorization/audit_m4_2_authorization_preparation.py`
- Modify: `evals/m4/authorization/audit_m4_2_gate_iv_b_protocol_proof.py`
- Modify: `tests/test_m4_2_gate_iv_b_protocol_proof.py`
- Test: `tests/test_m4_2_authorization_preparation.py`

**Interfaces:**
- Consumes: B3/B4 Git objects, proof artifact, M4.2 manifest, cases, variants, protocol, rubric, helper, and the three new schemas.
- Produces: one non-authorizing evidence artifact and deterministic in-memory projections that cannot validate as authorization/control instances.

- [ ] **Step 1: Implement strict, offline input loading**

Reject BOM, duplicate JSON keys, non-finite numbers, wrong types including bool/int confusion, missing Git objects, source-byte drift, schema drift, and any path escaping the repository. Do not import network, subprocess task surfaces, M4.1 builders, or filesystem writers.

- [ ] **Step 2: Recompute matrix and request bindings**

Read the frozen M4.2 manifest and source Git blobs. Recompute task/batch order, case and arm cardinality, request-binding frames, aggregate hashes, and all 60 case/protocol/variant/rubric byte matches. Compare computed values with the preparation artifact; never accept a stored `passed=true` as evidence.

- [ ] **Step 3: Implement non-instance projections**

Return projection objects with `projection_only=true`, `authorization_token=null`, `authorization_token_status=NOT_ISSUED`, `fresh_execution_authorized=false`, `claim_authorized=false`, and exact future task/batch/root structure. Assert each projection deliberately fails the corresponding future-instance status/authority/token requirements.

- [ ] **Step 4: Enforce whole-matrix and visibility policies**

Recompute the first-claim, partial-claim, second-claim, post-claim-failure, six batch-failure, and cross-task visibility projections from pure state transitions. Keep all actual counters zero and every forbidden path absent.

- [ ] **Step 5: Extend the Gate IV-B fuse without changing its proof artifact**

Pin B4 and the proof blob, permit only the named preparation schema/artifact/auditor/test/plan/workflow files as successor preparation state, and continue to reject the exact authorization/control instances, token, claim, execution/results roots, results manifest, and M5. Verify the original B3-to-B4 closure change set from immutable Git objects instead of treating successor files as part of B4.

- [ ] **Step 6: Create the provisional preparation artifact**

Set:

```json
{
  "preparation_kind": "AUTHORIZATION_SCHEMA_AND_PROJECTION_ONLY",
  "delivery": {"status": "PENDING_EXACT_HEAD_CI", "accepted_candidate_head": null, "push": null, "pull_request": null},
  "decision": "PENDING_M4_2_AUTHORIZATION_PREPARATION_EXACT_HEAD_CI",
  "status": "M4_2_AUTHORIZATION_PREPARATION_LOCAL_PASSED_NOT_AUTHORIZED"
}
```

Include B3 and B4 run/job/log evidence, schema raw hashes, recomputed matrix/request/policy projections, all-zero negative authority, `findings=[]`, and `auditor_side_effects=[]`. Do not include a valid token or executable instance.

- [ ] **Step 7: Run focused gates and commit**

```powershell
python -X utf8 -m unittest tests.test_m4_2_authorization_preparation tests.test_m4_2_gate_iv_b_protocol_proof -v
python -X utf8 evals/m4/authorization/audit_m4_2_authorization_preparation.py
python -X utf8 evals/m4/authorization/audit_m4_2_gate_iv_b_protocol_proof.py
python -X utf8 evals/m4/audit_results.py --expect-not-run
git add evals/m4/authorization/m4.2/authorization-preparation.json evals/m4/authorization/audit_m4_2_authorization_preparation.py evals/m4/authorization/audit_m4_2_gate_iv_b_protocol_proof.py tests/test_m4_2_authorization_preparation.py tests/test_m4_2_gate_iv_b_protocol_proof.py
git commit -m "eval(m4): prepare M4.2 authorization without issuance"
```

---

### Task 4: AP5 Dual-Platform Fail-Closed CI

**Files:**
- Modify: `.github/workflows/m1-validation.yml`
- Test: `tests/test_m4_2_authorization_preparation.py`

**Interfaces:**
- Consumes: the provisional candidate implementation.
- Produces: one Ubuntu/Windows preparation job pair plus preserved Gate IV-B replay and current lifecycle validation.

- [ ] **Step 1: Compile and run the preparation contract on both platforms**

Add `m4-2-authorization-preparation` with Ubuntu and Windows runners. Compile the auditor, run focused preparation tests, full current lifecycle tests, the frozen Gate IV-B replay, both auditors twice with byte-identical stdout, and `audit_results.py --expect-not-run` under `set -euo pipefail` or PowerShell stop-on-error semantics.

- [ ] **Step 2: Reverify all 60 request bundles**

Run `prepare_m4_2_request_bundles.ps1 -SelfTest` and `-CheckAll` under PowerShell 7 on Ubuntu and Windows PowerShell 5.1 on Windows. Require `status=VERIFIED`, `checked_task_count=60`, `mismatches=[]`, and `side_effects=[]`.

- [ ] **Step 3: Assert hard absence and clean checkout**

Check every forbidden exact/prefix path, require no `AUTHORIZED_UNCONSUMED` or `sha256:` token in the preparation artifact, run `git diff --check`, then require `git status --porcelain --untracked-files=all` to be empty.

- [ ] **Step 4: Run local lifecycle gates and commit**

```powershell
python -X utf8 -m py_compile evals/m4/authorization/audit_m4_2_authorization_preparation.py
python -X utf8 -m unittest discover -s tests -v
powershell -NoProfile -ExecutionPolicy Bypass -File evals/m4/execution/prepare_m4_2_request_bundles.ps1 -SelfTest
powershell -NoProfile -ExecutionPolicy Bypass -File evals/m4/execution/prepare_m4_2_request_bundles.ps1 -CheckAll
python -X utf8 evals/m4/audit_results.py --expect-not-run
git diff --check
git add .github/workflows/m1-validation.yml tests/test_m4_2_authorization_preparation.py
git commit -m "ci(m4): gate M4.2 authorization preparation"
```

---

### Task 5: AP6 Candidate Exact-HEAD Acceptance

**Files:**
- No new file paths; candidate HEAD contains Tasks 1–4 only.

**Interfaces:**
- Consumes: clean locally accepted candidate HEAD.
- Produces: Draft stacked PR and two exact-HEAD CI records for the same candidate SHA.

- [ ] **Step 1: Audit candidate scope and create the Draft PR**

Require `git status --short` clean after commit, verify `git merge-base --is-ancestor ad67a79f39685937466d3a49d30c6a5117e2810c HEAD`, push the successor branch, and create a Draft PR with base `codex/m4-cross-engineering-forward-evaluation-m4.2-gate-iv-b-protocol-proof`. Do not merge PR #7.

- [ ] **Step 2: Require candidate push and PR CI**

Wait for both runs at the exact candidate SHA. Require every job conclusion `success`, both preparation jobs present, both Gate IV-B replay jobs present, PowerShell 5.1 and 7 each verified 60/60, zero side effects, and all forbidden paths absent.

- [ ] **Step 3: Scan raw logs**

Capture exact `gh run view <run-id> --log` bytes and require zero occurrences of `FAIL:`, `FAILED (`, `Traceback`, and `##[error]`. A nonzero marker, failed job, wrong SHA, or missing platform stops the work package before closure.

---

### Task 6: AP6–AP7 Closure and Final Non-Authorization Decision

**Files:**
- Modify: `evals/m4/authorization/m4.2/authorization-preparation.json`
- Modify: `STATUS.md`
- Modify: `tests/test_m3_r5_erratum.py`

**Interfaces:**
- Consumes: accepted candidate push/PR exact-HEAD evidence.
- Produces: one closure commit recording only candidate evidence; its own CI remains an external exact-HEAD acceptance gate.

- [ ] **Step 1: Bind candidate delivery and emit the only allowed decision**

Replace provisional delivery with the exact candidate HEAD, push/PR run IDs, 13/13 expected jobs after the new matrix pair is added, job IDs/names/conclusions, raw log byte lengths/SHA-256 values, and zero markers. Set:

```text
decision = APPROVE_M4_2_SEPARATE_AUTHORIZATION_WORK_PACKAGE_ONLY
status   = M4_2_AUTHORIZATION_PREPARATION_PASSED_NOT_AUTHORIZED
```

- [ ] **Step 2: Update STATUS without claiming authorization**

Record the preparation artifact, immutable B3/B4 evidence, candidate CI, projection-only state, and final decision. Keep authorization/control instances absent, token not issued, claim absent, contexts/tasks/results/judge/aggregation zero or not run, closure not run, and M5 not started. State that a separate M4.2 one-shot authorization work package is required for any continuation.

- [ ] **Step 3: Run closure gates and commit only explicit paths**

```powershell
python -X utf8 -m unittest tests.test_m4_2_authorization_preparation tests.test_m4_2_gate_iv_b_protocol_proof tests.test_m3_r5_erratum -v
python -X utf8 -m unittest discover -s tests -v
python -X utf8 evals/m4/authorization/audit_m4_2_authorization_preparation.py
python -X utf8 evals/m4/authorization/audit_m4_2_gate_iv_b_protocol_proof.py
python -X utf8 evals/m4/audit_results.py --expect-not-run
git diff --check
git add evals/m4/authorization/m4.2/authorization-preparation.json STATUS.md tests/test_m3_r5_erratum.py
git commit -m "review(m4): close authorization preparation without authorization"
```

- [ ] **Step 4: Require closure push and PR exact-HEAD CI, then stop**

Push the closure commit and wait for its push and PR runs. Require every job successful, both platforms 60/60, byte-identical auditor output, clean checkouts, and zero raw-log markers. Do not create a third evidence commit, authorization instance, token, claim, execution path, result path, judge output, aggregate, closure claim, or M5 artifact.

## Self-Review Checklist

- [ ] The branch starts exactly at B4 and the Draft PR remains stacked on PR #7's head branch.
- [ ] B3 and B4 each bind separate push/PR 11/11 CI evidence and raw-log hashes with zero markers.
- [ ] The Gate IV-B proof artifact remains blob-identical at `d3fe975431f2e4584a52ee5305b169f5b5d29268`.
- [ ] Future authorization/control schemas are closed, but no instance or writer exists.
- [ ] The preparation artifact contains no valid token and no executable authorization state.
- [ ] Matrix, request bindings, source bytes, claim semantics, failure policy, and visibility are recomputed independently.
- [ ] Every actual counter is zero and every forbidden future path is absent.
- [ ] Gate IV-B replay, full lifecycle, NOT_RUN audit, PowerShell 5.1/7 60/60 checks, auditor repeatability, and clean-tree gates pass.
- [ ] Candidate and closure push/PR CI are exact-head true green with zero raw-log markers.
- [ ] Final decision is only `APPROVE_M4_2_SEPARATE_AUTHORIZATION_WORK_PACKAGE_ONLY` and M4.2 remains not authorized.
