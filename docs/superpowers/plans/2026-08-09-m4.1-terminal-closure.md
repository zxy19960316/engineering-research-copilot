# M4.1 Stopped Terminal Closure Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Prove that the single consumed M4.1 claim ended in an authentic, immutable, closed `STOPPED_PROTOCOL_FAILURE_PRESERVED` state without dispatch, result, retry, repair, judge, aggregation, M4 closure, or M5 activity.

**Architecture:** Preserve the two evidence files introduced by exact baseline `80b54697c3e27a5dad0a24d5318ce26c8fe46141` byte-for-byte. Add a read-only terminal auditor that reuses the frozen M4.1 protocol validators without rerunning the now-invalid unconsumed authorization gate, then split CI into an isolated pre-claim regression channel at `c370a3ce21661f0dbf2cdd153b8761d8fce71f9b` and a current terminal-state channel at HEAD. Keep every predecessor implementation path frozen and make `STATUS.md` describe the consumed, stopped lifecycle truthfully.

**Tech Stack:** Python 3.13 standard library, `unittest`, Git, Git worktrees, GitHub Actions, canonical UTF-8 JSON, SHA-256, Ubuntu and Windows runners.

## Global Constraints

- Start branch `codex/m4-cross-engineering-forward-evaluation-m4.1-terminal-closure` from exact commit `80b54697c3e27a5dad0a24d5318ce26c8fe46141`.
- Preserve `evals/m4/execution/m4.1/launch-claim.json` at SHA-256 `c16a2e53aa2e9215e2325464d547356afdb73897bfc7d29605e0105b9987b3c6` and byte length `24078`.
- Preserve `evals/m4/execution/m4.1/execution-terminal.json` at SHA-256 `7305d71ba94cd209f5bb0cb2c977db3bb157d95b907f8f59df9133c192f4d66e` and byte length `3707`.
- Preserve raw failure-evidence string SHA-256 `61a18842ac13637f9ba71a5dac6547d1fcbd4355127f97ea213e0ea44941f9c5`.
- Preserve claim ID `32e0df57-a8d2-5c19-9ffc-da69997686e8`, claim count `1`, timestamp `2026-08-08T14:28:37Z`, and `CONSUMED` authorization state.
- Preserve failed stage `post_claim_dual_confirmation`, failed batch `M4.1-BATCH-NUC`, failed task `M4.1-NUC-A-F`, and terminal state `STOPPED_PROTOCOL_OR_INFRASTRUCTURE_FAILURE`.
- Never delete, recreate, overwrite, normalize, reserialize, retimestamp, or otherwise alter either evidence file.
- Never call `--consume`, reset the token, resume M4.1, create an M4.1 result root, or create a dispatch receipt, raw final, results manifest, judge artifact, aggregate, closure artifact, or M5 artifact.
- Keep tasks, threads, attempts, finalizations, results, retries, repairs, followups, judge calls, aggregation calls, and side effects exactly zero.
- Keep `successor_revision_required=true`; M4.2 remains required and unauthorized.
- Preserve the red exact-HEAD evidence run `31262297707`; do not rewrite it as a pass.
- Keep development plans and evaluation artifacts outside `skills/engineering-research-copilot/`.
- Push and open a PR only after local gates pass; never amend, rebase, force-push, merge, or modify `main`.

---

## File map

- `docs/superpowers/plans/2026-08-09-m4.1-terminal-closure.md`: this bounded implementation and review plan.
- `STATUS.md`: incident record, stopped lifecycle state, zero counters, old red CI, later fixed CI, and M4.2 authorization boundary.
- `evals/m4/execution/audit_m4_1_terminal.py`: read-only exact-evidence and closed-terminal auditor.
- `tests/test_m4_1_terminal.py`: semantic mutation, absence, repeatability, and read-only tests.
- `.github/workflows/m1-validation.yml`: isolated pre-claim regression plus current terminal validation on Ubuntu and Windows.

## Frozen interface

`audit_m4_1_terminal.py` exports:

```python
def audit_terminal(
    repo_root: Path = REPO_ROOT,
    *,
    claim_path: Path | None = None,
    terminal_path: Path | None = None,
    results_base: Path | None = None,
    results_manifest_path: Path | None = None,
    platform_observations_path: Path | None = None,
    verify_git: bool = True,
) -> dict[str, object]:
    """Return one deterministic terminal-preservation result without writing."""
```

The successful result is exactly a compact, sorted JSON object with these stable fields:

```json
{"aggregation_calls":0,"attempts":0,"claim_count":1,"claim_id":"32e0df57-a8d2-5c19-9ffc-da69997686e8","claim_sha256":"c16a2e53aa2e9215e2325464d547356afdb73897bfc7d29605e0105b9987b3c6","errors":[],"failed_stage":"post_claim_dual_confirmation","finalizations":0,"followups":0,"judge_calls":0,"repairs":0,"result_root_count":0,"results":0,"retries":0,"side_effects":0,"status":"M4_1_STOPPED_PROTOCOL_FAILURE_PRESERVED","successor_revision_required":true,"tasks":0,"terminal_sha256":"7305d71ba94cd209f5bb0cb2c977db3bb157d95b907f8f59df9133c192f4d66e","threads":0,"token":"CONSUMED"}
```

The CLI prints that object using ASCII-safe canonical JSON and exits `0` only for the preserved status with no errors. It accepts no mutation or execution option.

## Task 1: Record the post-claim incident

**Files:**
- Create: `docs/superpowers/plans/2026-08-09-m4.1-terminal-closure.md`
- Modify: `STATUS.md`

**Interfaces:**
- Consumes: exact evidence commit `80b54697...`, red CI run `31262297707`, and the two frozen raw hashes.
- Produces: a human-readable incident state that does not imply readiness, acceptance, or permission to continue.

- [ ] **Step 1: Verify the evidence before any edit**

Run:

```powershell
Get-FileHash -Algorithm SHA256 evals/m4/execution/m4.1/launch-claim.json
Get-FileHash -Algorithm SHA256 evals/m4/execution/m4.1/execution-terminal.json
git diff --exit-code 80b54697c3e27a5dad0a24d5318ce26c8fe46141 HEAD -- evals/m4/execution/m4.1/launch-claim.json evals/m4/execution/m4.1/execution-terminal.json
```

Expected: the two fixed hashes above and an empty diff.

- [ ] **Step 2: Replace the stale active M4.1 status**

Set the top-level state to all four exact labels:

```text
M4_1_STOPPED_PROTOCOL_FAILURE_PRESERVED
M4_1_AUTHORIZATION_CONSUMED
M4_1_TASKS_NOT_DISPATCHED
M4_2_REQUIRED
```

Record the claim ID/hash, terminal hash, `post_claim_dual_confirmation`, all-zero execution/result counters, old red run `31262297707`, fixed terminal-closure CI as `PENDING`, no M4.1 continuation or rerun, M4.2 unauthorized, and judge/aggregation/M4 closure/M5 not run.

- [ ] **Step 3: Commit the incident record**

```powershell
git add -- docs/superpowers/plans/2026-08-09-m4.1-terminal-closure.md STATUS.md
git commit -m "docs: record M4.1 post-claim confirmation incident"
```

Expected: only the plan and `STATUS.md` are committed; both frozen evidence files remain byte-identical.

## Task 2: Add the immutable terminal auditor test-first

**Files:**
- Create: `tests/test_m4_1_terminal.py`
- Create: `evals/m4/execution/audit_m4_1_terminal.py`

**Interfaces:**
- Consumes: `audit_m4_1.audit_execution(..., verify_git=False)` for frozen schema/content validation, then adds exact terminal-closure semantics.
- Produces: `audit_terminal(...) -> dict[str, object]` and `main(argv) -> int`.

- [ ] **Step 1: Write the failing repository test**

```python
def test_repository_terminal_is_preserved_read_only_and_repeatable(self) -> None:
    before_status = git_status()
    before_claim = CLAIM_PATH.read_bytes()
    before_terminal = TERMINAL_PATH.read_bytes()
    first = terminal.audit_terminal(REPO_ROOT)
    second = terminal.audit_terminal(REPO_ROOT)
    self.assertEqual(first, second)
    self.assertEqual(first["status"], "M4_1_STOPPED_PROTOCOL_FAILURE_PRESERVED")
    self.assertEqual(first["errors"], [])
    self.assertEqual(before_status, git_status())
    self.assertEqual(before_claim, CLAIM_PATH.read_bytes())
    self.assertEqual(before_terminal, TERMINAL_PATH.read_bytes())
```

- [ ] **Step 2: Write semantic mutation tests**

Copy evidence into a temporary root, mutate exactly one item at a time, and require stable errors for: claim SHA/count/token; terminal SHA/claim binding/state/stage; undecodable failure evidence; missing `authorization_already_claimed`; any nonzero counter; nonempty attempted tasks/receipts/raw finals; successor flag false; any later gate opened; any result root, results manifest, or platform-observation root.

- [ ] **Step 3: Run the red test**

Run: `python -X utf8 -m unittest tests.test_m4_1_terminal -v`

Expected before implementation: import failure for `audit_m4_1_terminal`; repository bytes remain unchanged.

- [ ] **Step 4: Implement exact raw-byte and protocol checks**

The implementation must first hash the two raw files and compare both length and SHA-256. Then call:

```python
protocol_result = protocol.audit_execution(
    repo_root,
    claim_path=claim_path,
    results_base=results_base,
    terminal_path=terminal_path,
    results_manifest_path=results_manifest_path,
    verify_git=False,
)
```

Require protocol status `STOPPED_PROTOCOL_OR_INFRASTRUCTURE_FAILURE`, token `CONSUMED`, all counters zero, claim and terminal present, result root count zero, and successor revision required.

- [ ] **Step 5: Decode and verify the observed failure**

Require a `base64:` prefix, strict Base64 decode, a closed JSON object, `code=post_claim_dual_confirmation_failed`, `execution_status=INVALID`, and `authorization_already_claimed` inside `authorization_errors`. Also require the exact raw-evidence string SHA-256.

- [ ] **Step 6: Verify absence and Git preservation**

Require `evals/m4/results/m4.1`, `evals/m4/results-manifest.json`, and `evals/m4/execution/m4.1/platform-observations` absent. With `verify_git=True`, require baseline `80b54697...` to be an ancestor and both evidence paths to have no raw or textual diff from that baseline.

- [ ] **Step 7: Run focused tests and audit**

```powershell
python -X utf8 -m unittest tests.test_m4_1_terminal -v
python -X utf8 evals/m4/execution/audit_m4_1_terminal.py
```

Expected: all tests pass and the compact result equals the frozen interface result.

- [ ] **Step 8: Commit the audit**

```powershell
git add -- evals/m4/execution/audit_m4_1_terminal.py tests/test_m4_1_terminal.py
git commit -m "test: add immutable M4.1 terminal audit"
```

## Task 3: Split CI by lifecycle state

**Files:**
- Modify: `.github/workflows/m1-validation.yml`

**Interfaces:**
- Consumes: pre-claim commit `c370a3ce21661f0dbf2cdd153b8761d8fce71f9b` and current terminal auditor.
- Produces: one predecessor channel and one exact-HEAD terminal channel without evaluating contradictory lifecycle assertions in one checkout.

- [ ] **Step 1: Compile the new auditor**

Add `evals/m4/execution/audit_m4_1_terminal.py` to the existing compile list.

- [ ] **Step 2: Run pre-claim regressions only in an isolated worktree**

In the Ubuntu `validate` job, add a detached worktree at `c370a3c...`; inside it run the original full unit discovery, M4.1 preparation builder/audit, authorization builder/audit, execution protocol tests/audit, launch-readiness tests/writer checks/audit, PowerShell request preflight, and the claim/terminal/result-absence assertion. Remove that worktree in an `if: always()` cleanup step.

- [ ] **Step 3: Run current historical and terminal tests at HEAD**

Replace unconditional full discovery at current HEAD with deterministic discovery excluding only these predecessor-only modules:

```python
PRECLAIM_ONLY = {
    "test_m4_1_preparation",
    "test_m4_1_authorization_builder",
    "test_m4_1_authorization",
    "test_m4_1_execution",
    "test_m4_1_launch_readiness",
}
```

Run every other `tests/test_*.py`, including `test_m4_1_terminal`. Keep all existing M1, M2, M3, M4.0 replay/audit and frozen-path gates.

- [ ] **Step 4: Replace current claim-absence gates**

At current HEAD run the dedicated terminal test/auditor, assert the authorization token is consumed with claim count one, assert all execution/result counters zero, assert dispatch receipt/raw final/result root/results manifest absent, and assert both frozen evidence paths match `80b54697...`. Do not run authorization `READY_UNCONSUMED`, execution `READY_UNCLAIMED`, launch readiness, or claim-absence assertions in the current checkout.

- [ ] **Step 5: Make the matrix lifecycle-aware**

On both `ubuntu-latest` and `windows-latest`, retain historical M3 and M4.0 tests/frozen gates, remove current-checkout predecessor-only M4.1 tests, then run `tests.test_m4_1_terminal` and `audit_m4_1_terminal.py`.

- [ ] **Step 6: Validate workflow syntax and local gates**

```powershell
python -X utf8 -m unittest tests.test_m4_1_terminal -v
python -X utf8 evals/m4/execution/audit_m4_1_terminal.py
python -X utf8 -c "import yaml, pathlib; yaml.safe_load(pathlib.Path('.github/workflows/m1-validation.yml').read_text(encoding='utf-8')); print('VALID')"
```

Expected: tests/audit pass, YAML prints `VALID`, frozen evidence hashes remain unchanged, and `git diff --check` is clean.

- [ ] **Step 7: Commit CI lifecycle separation**

```powershell
git add -- .github/workflows/m1-validation.yml
git commit -m "ci: validate M4.1 lifecycle states separately"
```

## Task 4: Obtain exact-HEAD CI and close status

**Files:**
- Modify: `STATUS.md`

**Interfaces:**
- Consumes: a successful GitHub Actions run for the terminal-closure implementation commit.
- Produces: final documentary closure without authorizing M4.2.

- [ ] **Step 1: Push the three implementation commits normally**

```powershell
git push -u origin codex/m4-cross-engineering-forward-evaluation-m4.1-terminal-closure
```

Expected: no force update; `main` and the original M4.1 branch remain unchanged.

- [ ] **Step 2: Require the implementation exact-HEAD run green**

Wait for validate, Ubuntu, and Windows jobs. Record the run and job IDs only if every required check succeeds. If any check fails, preserve that run and fix only the terminal-closure implementation; never alter either frozen evidence file or rerun M4.1.

- [ ] **Step 3: Replace only the pending CI marker**

Record the successful terminal-closure run ID and job conclusions. Keep run `31262297707` as the original red consumed-state record, all counters zero, M4.1 continuation forbidden, and M4.2 unauthorized.

- [ ] **Step 4: Commit final documentary closure**

```powershell
git add -- STATUS.md
git commit -m "docs: close M4.1 stopped terminal evidence"
git push
```

- [ ] **Step 5: Require final exact-HEAD CI green**

Wait for the final documentation HEAD. Require validate, Ubuntu, and Windows success. Re-run local terminal audit and raw SHA checks after CI.

## Task 5: PR and independent review gate

**Files:**
- No repository file is modified unless review identifies an in-scope terminal-closure defect.

**Interfaces:**
- Consumes: final green branch HEAD.
- Produces: one draft or ready PR containing only the four ordered commits and an independent review result.

- [ ] **Step 1: Audit publication scope**

```powershell
git status --short
git log --oneline 80b54697c3e27a5dad0a24d5318ce26c8fe46141..HEAD
git diff --name-status 80b54697c3e27a5dad0a24d5318ce26c8fe46141..HEAD
git diff --exit-code 80b54697c3e27a5dad0a24d5318ce26c8fe46141 HEAD -- evals/m4/execution/m4.1/launch-claim.json evals/m4/execution/m4.1/execution-terminal.json
```

Expected: clean tree; four commits in the required order; only plan, status, auditor, tests, and workflow changed; evidence diff empty.

- [ ] **Step 2: Open one terminal-closure PR**

Describe the stopped outcome, immutable hashes, dual-channel CI design, old red run, new green runs, and explicit exclusions. Do not claim READY or ACCEPTED and do not include M4.2 implementation.

- [ ] **Step 3: Complete independent review**

Require a reviewer to check spec conformance, frozen bytes, semantic mutation coverage, CI lifecycle separation, Ubuntu/Windows evidence, and later-gate closure. Resolve only in-scope findings; do not merge without separate authorization.

## Self-review

- Spec coverage: A1 is covered by fixed hashes and Git preservation; A2 by the dedicated auditor/tests; A3 by the worktree/current-head CI split; A4 by two documentary commits; A5 by ordered commits, normal push, PR, and review gate.
- Placeholder scan: the only temporary status value is the intentional machine state `PENDING` before a GitHub run ID exists; Task 4 replaces it with observed evidence.
- Type consistency: every task uses the same `audit_terminal(...) -> dict[str, object]` interface and exact status `M4_1_STOPPED_PROTOCOL_FAILURE_PRESERVED`.
- Authorization boundary: no step calls a writer with `--consume`, dispatches a task, creates results, authorizes M4.2, judges, aggregates, closes M4, starts M5, merges, or modifies `main`.
