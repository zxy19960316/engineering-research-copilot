# M4.2 Gate IV-A Independent Review Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Independently verify the frozen M4.2 preparation and its CI-integrity repair, then record `M4_2_GATE_IV_A_REVIEW_PASSED_NOT_AUTHORIZED` if and only if the closed review has no findings or reviewer side effects.

**Architecture:** Treat commit `941602180c75c4ae16edfc927f6c39b8420fb45c` and tree `43357e6fa252abbb84095aebb577974974527791` as the immutable reviewed object graph. Handwrite a closed review artifact from Git blobs, manifests, GitHub Actions metadata, and raw logs; validate it with a separate read-only auditor that never imports or executes the M4.2 builder or preparation auditor. Keep review authority distinct from execution authority: the only approving decision is permission to prepare a later Gate IV-B protocol proof.

**Tech Stack:** Python 3.13 standard library, closed JSON Schema Draft 2020-12, `unittest`, Git object plumbing, GitHub Actions, Bash strict mode, Windows PowerShell 5.1, PowerShell 7, UTF-8/LF.

## Global Constraints

- Review exactly `941602180c75c4ae16edfc927f6c39b8420fb45c` on branch `codex/m4-cross-engineering-forward-evaluation-m4.2-successor-preparation`; do not review `main`, an M4.1 branch, a worktree-only projection, or a regenerated builder output.
- Create the review branch `codex/m4-cross-engineering-forward-evaluation-m4.2-gate-iv-a-independent-review` from that exact commit and target the stacked Draft PR at `codex/m4-cross-engineering-forward-evaluation-m4.2-successor-preparation`.
- Preserve implementation evidence HEAD `242490a5d0d4e9bc52f21263d8d6780830ab1c8f`, push run `31316090614`, PR run `31316093185`, and seven successful jobs in each run.
- Preserve closure evidence HEAD `941602180c75c4ae16edfc927f6c39b8420fb45c`, push run `31316592412`, PR run `31316593775`, and seven successful jobs in each run.
- Preserve false-green runs `31311637459` and `31313212880` as `NOT_ACCEPTED`; a workflow conclusion of success must not erase the recorded Windows test failure.
- Independently derive 60 tasks, six batches, 60 unique request bindings, blind IDs `M4-J121..M4-J180`, zero reused task IDs, direct lineage M4.1, root lineage M4.0, all-false authority, all-zero counters, absent future paths, preserved M4.1 terminal evidence, and `NOT_RUN` results.
- Do not modify `evals/m4/build_m4_2_preparation.py`, `evals/m4/audit_m4_2_preparation.py`, either M4.2 preparation manifest file, `evals/m4/execution/prepare_m4_2_request_bundles.ps1`, any M4.0/M4.1 artifact, any case/variant/rubric/shared-protocol artifact, or any preparation commit from PR #3.
- Do not create or modify an execution authorization, execution control, launch claim, task, result, judge, aggregate, closure, or M5 artifact.
- Keep the auditor offline and read-only. It may launch only read-only Git subprocesses, may not write the review artifact, may not invoke either M4.2 preparation implementation module, and must emit byte-identical JSON on repeated runs.
- Any preparation defect found during this review changes the outcome to `BLOCKED`; do not repair preparation on this branch.
- Keep `fresh_execution_authorized=false`, `authorization_created=false`, and `execution_created=false` in every passing state.

---

## File Map

- `docs/superpowers/plans/2026-08-09-m4.2-gate-iv-a-independent-review.md`: exact review scope, evidence sources, implementation sequence, publication gate, and stop boundary.
- `evals/m4/authorization/m4.2/gate-iv-a-review.schema.json`: closed contract for the independent review record.
- `evals/m4/authorization/m4.2/gate-iv-a-review.json`: handwritten evidence record; no builder may generate it.
- `evals/m4/authorization/audit_m4_2_gate_iv_a_review.py`: offline, read-only Git-object and artifact auditor.
- `tests/test_m4_2_gate_iv_a_review.py`: positive, mutation, repeatability, path-absence, workflow, and side-effect tests.
- `.github/workflows/m1-validation.yml`: isolated reviewed-baseline job plus Ubuntu/Windows current-review jobs.
- `STATUS.md`: current Gate IV-A result and explicit absence of all execution authority and later artifacts.
- `tests/test_m3_r5_erratum.py`: full-document lifecycle consistency checks, only if the current-status assertions require updating.

### Task 1: Freeze the Closed Review Contract Red-First

**Files:**
- Create: `evals/m4/authorization/m4.2/gate-iv-a-review.schema.json`
- Create: `tests/test_m4_2_gate_iv_a_review.py`

**Interfaces:**
- Consumes: frozen baseline commit/tree/branch, reviewed Git blobs, CI run and job metadata, raw-log digests, M4.2 preparation facts, M4.1 terminal evidence, and forbidden-path observations.
- Produces: `audit_review(repo_root: Path, review_path: Path | None = None, verify_git: bool = True) -> dict[str, object]` with a terminal `status`, sorted `errors`, and empty `reviewer_side_effects`.

- [ ] **Step 1: Define the exact root contract**

Require exactly these root keys and set `additionalProperties: false`:

```python
REVIEW_KEYS = {
    "schema_version", "reviewed_head", "reviewed_tree", "reviewed_branch",
    "reviewed_artifacts", "ci_evidence", "matrix_checks", "identity_checks",
    "lineage_checks", "request_binding_checks", "ci_integrity_checks",
    "historical_preservation", "zero_state", "lifecycle_requirements",
    "findings", "limitations", "reviewer_side_effects", "decision", "status",
}
```

Freeze `schema_version=m4.2-gate-iv-a-independent-review-v1`, `reviewed_head=9416021...`, `reviewed_tree=43357e6...`, and the reviewed preparation branch. Every nested object is closed; hashes match `^[0-9a-f]{64}$`, Git object IDs match `^[0-9a-f]{40}$`, run/job IDs are positive integers, and arrays use explicit item schemas.

- [ ] **Step 2: Freeze the non-authorization decision**

Require these values together:

```python
EXPECTED_DECISION = "APPROVE_M4_2_GATE_IV_B_PROTOCOL_PROOF_ONLY"
EXPECTED_STATUS = "M4_2_GATE_IV_A_REVIEW_PASSED_NOT_AUTHORIZED"
EXPECTED_LIFECYCLE = {
    "fresh_execution_authorized": False,
    "authorization_created": False,
    "execution_created": False,
    "claim_created": False,
    "judge": "NOT_RUN",
    "aggregation": "NOT_RUN",
    "closure": "NOT_RUN",
    "m5": "NOT_STARTED",
}
```

The schema must prevent `status=PASSED` when `findings` or `reviewer_side_effects` is nonempty and prevent any decision containing execution authorization.

- [ ] **Step 3: Specify every required mutation**

Add one focused test for each mutation: reviewed HEAD, reviewed tree, preparation manifest hash, helper hash, task count, batch count, task reuse, blind-ID reuse, direct lineage, root lineage, request binding, false-green relabeling, missing true-green evidence, authority set true, nonzero counter, authorization artifact presence, execution/result path presence, nonempty findings with passed status, nonempty reviewer side effects, and a decision that attempts to authorize execution.

- [ ] **Step 4: Run the red tests**

Run: `python -X utf8 -m unittest tests.test_m4_2_gate_iv_a_review -v`

Expected: fail because the review artifact and auditor do not exist.

### Task 2: Independently Recompute and Handwrite the Review Artifact

**Files:**
- Create: `evals/m4/authorization/m4.2/gate-iv-a-review.json`
- Modify: `tests/test_m4_2_gate_iv_a_review.py`

**Interfaces:**
- Consumes: `git show 9416021:<path>` bytes, `git ls-tree` object IDs, exact GitHub run/job metadata, and raw logs retrieved by the reviewer.
- Produces: one canonical UTF-8/LF JSON artifact with `findings=[]`, `reviewer_side_effects=[]`, and no executable authority.

- [ ] **Step 1: Bind reviewed artifacts by Git object and raw SHA-256**

Record at least the preparation manifest, preparation schema, preparation auditor, request helper, preparation tests, workflow, STATUS, M4.1 claim, M4.1 terminal, and M4.1 terminal auditor. For each entry, record `path`, `git_blob_oid`, `raw_sha256`, and `byte_length` derived directly from the reviewed commit.

- [ ] **Step 2: Recompute matrix, identity, lineage, and request bindings**

Parse the reviewed M4.2 and M4.1 manifest blobs directly. Recompute request bindings with the fixed frame without importing either M4.2 implementation module:

```python
frame = (
    "m4.2-request-binding-v1", task_id, source_task_id, root_task_id, blind_id,
    case_sha256, user_input_sha256, task_protocol_sha256,
    variant_instruction_sha256 or "NONE", rubric_sha256,
    execution_constraints_sha256,
)
request_binding = sha256(("\n".join(frame) + "\n").encode("utf-8")).hexdigest()
```

Record 60/60 matches, 60 unique hashes, six batches, IDs J121-J180, zero overlap with M4.0/M4.1, direct lineage M4.1, root lineage M4.0, all-false authority, and all-zero counters.

- [ ] **Step 3: Review raw CI evidence**

For the implementation and closure push/PR runs, record run ID, event, head, conclusion, seven job IDs/names/conclusions, full-log SHA-256, and banned-pattern scan counts for `FAIL:`, `FAILED (`, `Traceback`, and `##[error]`. Record Windows PowerShell 5.1 `request_bindings=60/60`. Record both false-green runs as `NOT_ACCEPTED` with their Windows failure observation; never infer acceptance from the run-level conclusion.

- [ ] **Step 4: Record CI-integrity regression facts**

Record that the reviewed workflow uses `shell: bash`, begins its compound Python gate with `set -euo pipefail`, contains no `continue-on-error`, and that the committed focused tests cover canonical-root aliasing, four raw CRLF mismatch classes, and sibling/symlink/junction escapes.

- [ ] **Step 5: Canonicalize the handwritten artifact**

Serialize with `indent=2`, `ensure_ascii=False`, sorted only where order is semantically irrelevant, and one final LF. Do not call the preparation builder or any generator for this artifact.

### Task 3: Implement the Independent Read-Only Auditor

**Files:**
- Create: `evals/m4/authorization/audit_m4_2_gate_iv_a_review.py`
- Modify: `tests/test_m4_2_gate_iv_a_review.py`

**Interfaces:**
- Consumes: the handwritten review JSON plus baseline Git objects read with `git cat-file`, `git show`, `git ls-tree`, and `git diff-tree`.
- Produces: canonical compact JSON with `status=M4_2_GATE_IV_A_REVIEW_PASSED_NOT_AUTHORIZED` only when every evidence, identity, permission, lifecycle, CI, and absence gate passes.

- [ ] **Step 1: Load strict JSON and validate the closed key graph**

Reject BOMs, duplicate JSON keys, non-object roots, wrong key sets, bool/int confusion, unknown nested keys, malformed hashes, and any artifact/schema disagreement. Do not import `build_m4_2_preparation` or `audit_m4_2_preparation`.

- [ ] **Step 2: Read the reviewed baseline directly from Git**

Use a replacement-disabled Git environment and `git show <reviewed_head>:<path>` for all reviewed data. Require the exact commit, tree, branch name, artifact blobs, raw hashes, and lengths. Require the baseline to be an ancestor of current HEAD, while allowing only the five new review files plus workflow, STATUS, and the optional status test to differ.

- [ ] **Step 3: Independently validate preparation facts**

Recompute matrix counts, identity disjointness, direct/root lineage, inherited bindings, request hashes, helper binding, all-false authority, zero counters, M4.1 terminal preservation, and results `NOT_RUN` from Git blobs. A preparation builder self-comparison is not an accepted proof.

- [ ] **Step 4: Fail closed on CI or permission drift**

Require exact run/job/log metadata, historical false-green labels, strict Bash controls, regression test presence, empty findings/side effects, absent future paths, and the protocol-proof-only decision. Return `BLOCKED` with sorted error codes for any mismatch.

- [ ] **Step 5: Prove byte stability and read-only behavior**

Run the auditor twice, compare stdout bytes, and compare `git status --porcelain --untracked-files=all` before/after. Require no filesystem mutation and no network-access primitive in the source.

### Task 4: Add Cross-Platform Review CI and Record Current Status

**Files:**
- Modify: `.github/workflows/m1-validation.yml`
- Modify: `STATUS.md`
- Modify if required: `tests/test_m3_r5_erratum.py`

**Interfaces:**
- Consumes: frozen baseline preparation at `9416021`, current review artifact/auditor/tests, current lifecycle suite, M4.1 terminal audit, results audit, helper, and path-absence gates.
- Produces: isolated baseline validation plus Ubuntu and Windows Gate IV-A review jobs without creating authority.

- [ ] **Step 1: Add the auditor to compilation and lifecycle validation**

Compile the new auditor, run focused review tests, run the auditor, run current lifecycle tests, run the M4.1 terminal auditor, and run `evals/m4/audit_results.py --expect-not-run`.

- [ ] **Step 2: Add an isolated reviewed-baseline preparation job**

Create a detached, `core.autocrlf=false` worktree at `9416021...`; run the baseline M4.2 preparation audit, focused preparation tests, results-not-run audit, and forbidden-path assertions; remove the worktree with an `always()` cleanup step.

- [ ] **Step 3: Add Ubuntu and Windows current-review jobs**

Use Bash strict mode for compound Python gates on both runners. On Windows also run Windows PowerShell 5.1 helper `-SelfTest` and `-CheckAll`, requiring 60/60 request bindings. End each job with a clean-worktree assertion.

- [ ] **Step 4: Record the closed current state**

Set the active revision to `M4.2 GATE_IV_A_REVIEW; M4_2_GATE_IV_A_REVIEW_PASSED_NOT_AUTHORIZED` only if the local review has no findings. Continue to state `fresh_execution_authorized=false`, `authorization=ABSENT`, `execution=ABSENT`, `claim=ABSENT`, `tasks=0`, `results=0`, `judge=NOT_RUN`, `aggregation=NOT_RUN`, `closure=NOT_RUN`, and `M5=NOT_STARTED`.

### Task 5: Validate, Publish the Stacked Draft PR, and Stop at Exact-HEAD CI

**Files:**
- Stage only the files listed in this plan.

**Interfaces:**
- Consumes: complete local diff and all local gates.
- Produces: one intentional commit, one pushed review branch, one stacked Draft PR, and exact-HEAD true-green evidence.

- [ ] **Step 1: Run the complete local gate**

Run focused review tests, the auditor twice, the current lifecycle suite, M4.1 terminal audit, results-not-run audit, PowerShell helper checks, YAML parse, Python compilation, forbidden-path assertions, and a prohibited-file diff audit. Expected: all pass; findings and side effects empty; no authorization/execution artifacts.

- [ ] **Step 2: Inspect and commit explicit paths**

```powershell
git add -- docs/superpowers/plans/2026-08-09-m4.2-gate-iv-a-independent-review.md evals/m4/authorization/m4.2/gate-iv-a-review.schema.json evals/m4/authorization/m4.2/gate-iv-a-review.json evals/m4/authorization/audit_m4_2_gate_iv_a_review.py tests/test_m4_2_gate_iv_a_review.py .github/workflows/m1-validation.yml STATUS.md
git commit -m "eval(m4): add independent M4.2 Gate IV-A review"
```

Add `tests/test_m3_r5_erratum.py` only if it actually changed. Never use a broad staging command.

- [ ] **Step 3: Push and open the stacked Draft PR**

Push `codex/m4-cross-engineering-forward-evaluation-m4.2-gate-iv-a-independent-review`, then create a Draft PR whose base is exactly `codex/m4-cross-engineering-forward-evaluation-m4.2-successor-preparation` and whose head is the review branch.

- [ ] **Step 4: Verify exact-HEAD CI and raw logs**

Require every job to succeed at the pushed commit, focused review and lifecycle suites to report OK, the auditor to report empty findings/side effects and zero forbidden paths, Windows PowerShell 5.1 to report 60/60, raw logs to contain none of the four banned failure markers, and authorization/execution paths to remain absent.

- [ ] **Step 5: Record the exact-HEAD review state and stop**

If recording the exact run requires a closure-only STATUS/artifact update, make one narrow evidence-record commit, push it, and require its own exact-HEAD CI. Stop immediately after the branch, stacked Draft PR, true-green exact-HEAD CI, and recorded review state all agree. Do not implement Gate IV-B, authorization, control, claim, contexts, or results.

## Self-Review Checklist

- [ ] Every required review field is present in a closed schema and no execution authority can be represented as passing.
- [ ] The review artifact is handwritten from Git objects and CI evidence, not generated by a preparation builder.
- [ ] The auditor never imports or executes either M4.2 preparation implementation module.
- [ ] Matrix, identity, lineage, binding, permission, zero-state, and lifecycle conclusions are independently recomputed.
- [ ] Implementation and closure push/PR runs, job IDs, log hashes, and false-green history are exact.
- [ ] Strict Bash, no `continue-on-error`, canonical-root, CRLF, and escape regressions are all checked.
- [ ] Every requested mutation causes `BLOCKED` or `INVALID`, never a passed status.
- [ ] Baseline preparation, current review, lifecycle, M4.1 terminal, results-not-run, PowerShell 5.1, forbidden-path, and clean-worktree gates all run in CI.
- [ ] Prohibited preparation and historical files are byte-identical to `9416021...`.
- [ ] The Draft PR is stacked on the preparation branch, not `main` or M4.1.
- [ ] No execution authorization, control, claim, task, result, judge, aggregate, closure, or M5 artifact is created.
