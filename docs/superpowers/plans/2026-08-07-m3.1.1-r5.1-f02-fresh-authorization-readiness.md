# M3.1.1 r5.1-f02 Fresh Authorization Readiness Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Those sub-skills are not installed in this session, so execute inline with the same review checkpoints and no delegation.

**Goal:** Prove, without side effects, that the frozen `r5.1-f02` replacement preparation is safe to receive a separate future fresh-context authorization.

**Architecture:** Extend generic authority regression coverage, then add a dedicated single-case authorization auditor that independently revalidates every frozen identity and dependency. Add a narrow dispatcher facade that calls the auditor before any callback but does not create tasks, authorize runs, or write artifacts. CI and a local validation record stop at `READY_FOR_FRESH_AUTHORIZATION`.

**Tech Stack:** Python 3.13, `unittest`, Git plumbing (`cat-file`, `show`, `rev-parse`, `diff`), GitHub Actions YAML, JSON manifests.

## Global Constraints

- Start from exact HEAD `bbf54721b090d9d91b269d88e31919ae00fb0a39` on branch `codex/m3.1.1-r5.1-f02-preparation` with run `31115643290` successful.
- Work on `codex/m3.1.1-r5.1-f02-fresh-authorization`.
- Preserve evidence HEAD `1b696bce53ee0a11163bfe4f91a9a49ab3af6f49` and `evals/m3/results/forward-r5/**` byte-for-byte.
- Preserve historical failed F02 task `019fd687-5575-7143-8cf3-1ab3069611f5` and every historical transaction, receipt, and model-final identity.
- Keep `new_fresh_run_authorized=false`, `reserved_task_id=null`, M3 `IN_PROGRESS`, r5 `BLOCKED_NOT_ACCEPTED`, and M4 `NOT_STARTED`.
- Do not launch or reserve a task, consume a final, create a result artifact, aggregate acceptance, close M3, start M4, merge, or push.

---

### Task 1: Record the accepted preparation baseline and complete generic authority regressions

**Files:**
- Modify: `STATUS.md`
- Modify: `tests/test_validate_m3_method_bundle.py`

**Interfaces:**
- Consumes: `validate_m3_bundle(bundle: Any) -> dict` and accepted preparation run `31115643290`.
- Produces: table-driven route-card and nuclear-overlay authority regression coverage without validator changes.

- [ ] **Step 1: Add operator drift to the existing route-card table**

```python
mutations = {
    "metric_id": "M-NOT-AUTHORITATIVE",
    "criterion_type": "pivot",
    "operator": ">=",
    "value": 0.51,
    "unit": "percent",
}
```

- [ ] **Step 2: Add route-specific overlay drift coverage**

Create a valid route-specific bundle with a nuclear overlay whose `additional_stop_conditions` initially copies `route_output.stop_conditions[0]`. Mutate `metric_id`, `operator`, `value`, and `unit` one at a time and require `nuclear_overlay_stop_condition_not_authoritative`.

- [ ] **Step 3: Run the two focused regressions**

Run:

```powershell
python -X utf8 -m unittest tests.test_validate_m3_method_bundle.ValidateM3MethodBundleTests.test_route_card_rejects_any_authoritative_condition_field_drift tests.test_validate_m3_method_bundle.ValidateM3MethodBundleTests.test_route_nuclear_overlay_rejects_any_authoritative_stop_condition_field_drift -v
```

Expected: both pass using the existing strict authority equality implementation.

- [ ] **Step 4: Update only already-observed status facts**

Record preparation exact-HEAD CI `PASSED`, HEAD `bbf54721b090d9d91b269d88e31919ae00fb0a39`, and run `31115643290`; keep fresh F02, aggregate acceptance, M3 closure, and M4 `NOT_RUN`/`NOT_STARTED`.

- [ ] **Step 5: Commit**

```powershell
git add -- STATUS.md tests/test_validate_m3_method_bundle.py
git commit -m "test: complete r5.1-f02 authority regressions"
```

### Task 2: Add a dedicated fail-closed authorization auditor and manifest

**Files:**
- Create: `evals/m3/forward-inputs-r5.1-f02/authorization-manifest.json`
- Create: `evals/m3/audit_forward_r5_1_f02_authorization.py`
- Create: `tests/test_audit_m3_forward_r5_1_f02_authorization.py`

**Interfaces:**
- Consumes: frozen preparation manifest, input binding, prompt, replacement contract, base contract, erratum, evidence HEAD, Git blobs, and empty replacement result root.
- Produces: `audit_authorization(manifest_path: str | Path) -> dict[str, Any]` with terminal status `ready_for_fresh_authorization` or `invalid`.

- [ ] **Step 1: Write failing tests for all identity and side-effect gates**

Cover baseline SHA, evidence HEAD, r5 tree, source raw/canonical/blob identities, prompt/contract/base-contract bindings, route authority, M2/eligibility receipts, historical task/root reuse, counters, future paths, result-root emptiness, and zero side effects. Patch only test-owned paths or subprocess results; do not bypass production identity validation in the valid case.

- [ ] **Step 2: Run the new test module and observe import failure**

Run:

```powershell
python -X utf8 -m unittest tests.test_audit_m3_forward_r5_1_f02_authorization -v
```

Expected: fail because the authorization module does not exist.

- [ ] **Step 3: Freeze an independent authorization manifest**

Use exactly:

```json
{
  "status": "ready_for_fresh_authorization",
  "preparation_baseline_head": "bbf54721b090d9d91b269d88e31919ae00fb0a39",
  "historical_evidence_head": "1b696bce53ee0a11163bfe4f91a9a49ab3af6f49",
  "revision": "r5.1-f02",
  "case_id": "m3-f02",
  "new_fresh_run_authorized": false,
  "reserved_task_id": null
}
```

Add exact path/hash references to the preparation manifest, input binding, prompt, replacement contract, and erratum; keep all counters zero and canonical future names unchanged.

- [ ] **Step 4: Implement the read-only auditor**

The auditor must use Git blobs at the declared evidence/baseline heads for immutable identities, compute canonical JSON SHA-256 deterministically, verify the base contract's path/OID/raw/canonical tuple, compare `forward-r5` to the evidence head, and count every result-root entry except zero-byte `.gitkeep` as an artifact. It must never write.

- [ ] **Step 5: Run the new tests**

Run the module from Step 2. Expected: all tests pass and the valid audit reports zero errors, counters, artifacts, and side effects.

- [ ] **Step 6: Commit**

```powershell
git add -- evals/m3/forward-inputs-r5.1-f02/authorization-manifest.json evals/m3/audit_forward_r5_1_f02_authorization.py tests/test_audit_m3_forward_r5_1_f02_authorization.py
git commit -m "eval: add r5.1-f02 authorization preflight"
```

### Task 3: Add a narrow single-case dispatcher and CI readiness gate

**Files:**
- Create: `evals/m3/dispatch_forward_r5_1_f02.py`
- Create: `tests/test_dispatch_forward_r5_1_f02.py`
- Modify: `.github/workflows/m1-validation.yml`

**Interfaces:**
- Consumes: `audit_authorization(...)` and an externally supplied callback.
- Produces: `preflight_then_callback(manifest_path, callback) -> dict` that invokes the callback at most once only after a valid preflight; it creates no task and writes nothing.

- [ ] **Step 1: Write dispatcher tests**

Require callback count zero when preflight is invalid and one when valid. The valid callback returns opaque test data only; the dispatcher must not process or persist it.

- [ ] **Step 2: Run tests and observe import failure**

```powershell
python -X utf8 -m unittest tests.test_dispatch_forward_r5_1_f02 -v
```

- [ ] **Step 3: Implement the narrow facade**

Reject every case/revision other than `m3-f02`/`r5.1-f02`, call the full authorization audit first, return `callback_invocations=0` on invalid input, and never call `consume_forward_r5_once` during readiness.

- [ ] **Step 4: Add CI integration**

Add both scripts to `py_compile` and add this step after the existing preparation audit:

```yaml
- name: Audit r5.1-f02 fresh authorization readiness without consumption
  run: python evals/m3/audit_forward_r5_1_f02_authorization.py
```

- [ ] **Step 5: Run focused tests and compile**

Expected: auditor and dispatcher modules compile; all focused tests pass.

- [ ] **Step 6: Commit**

```powershell
git add -- evals/m3/dispatch_forward_r5_1_f02.py tests/test_dispatch_forward_r5_1_f02.py .github/workflows/m1-validation.yml
git commit -m "ci: gate r5.1-f02 fresh authorization readiness"
```

### Task 4: Run complete local gates and record readiness

**Files:**
- Create: `evals/m3/results/2026-08-07-m3.1.1-r5.1-f02-authorization-readiness-validation.md`
- Modify: `STATUS.md`

**Interfaces:**
- Consumes: all implementation commits and local gate outputs.
- Produces: truthful local authorization-readiness evidence; no execution authority.

- [ ] **Step 1: Run compilation, focused tests, and full unittest discovery**

Expected: zero failures.

- [ ] **Step 2: Run M1/M2/M3 replay and regeneration gates**

Require M1 offline and machine-artifact replay valid, M2/M3 fixture regeneration diffs empty, and all frozen replays matched.

- [ ] **Step 3: Run package, Skill, blocked-state, preparation, and authorization audits**

Require each status valid/ready and no side effects.

- [ ] **Step 4: Verify immutable and scope boundaries**

```powershell
git diff --exit-code 1b696bce53ee0a11163bfe4f91a9a49ab3af6f49 -- evals/m3/results/forward-r5
git diff --check
```

Require the replacement result root to contain only zero-byte `.gitkeep`, all counters zero, and historical F02 retry count zero.

- [ ] **Step 5: Write the validation record and status**

Record branch, implementation HEAD, baseline/evidence heads, exact counts and gate outputs. State explicitly that this does not authorize or launch F02, consume a final, accept F02, aggregate evidence, close M3, or start M4.

- [ ] **Step 6: Commit and stop**

```powershell
git add -- STATUS.md evals/m3/results/2026-08-07-m3.1.1-r5.1-f02-authorization-readiness-validation.md
git commit -m "docs: record r5.1-f02 authorization readiness"
```

Do not push because the current task contains no push authorization. Final state must be `READY_FOR_FRESH_AUTHORIZATION`, not `fresh_run_authorized`.
